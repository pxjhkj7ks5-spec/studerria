<%
  const pathways = registrationPathways || { tracks: [], programs: [], admissions: [], links: [], courses: [] };
  const selectedTrackValue = typeof selectedTrack === 'string' && selectedTrack ? selectedTrack : 'bachelor';
  const selectedProgramValue = Number.isFinite(Number(selectedProgramId)) ? Number(selectedProgramId) : null;
  const selectedAdmissionValue = Number.isFinite(Number(selectedAdmissionId)) ? Number(selectedAdmissionId) : null;
  const selectedCampusValue = typeof selectedCampus === 'string' ? selectedCampus : '';
  const registerCourseText = lang === 'en'
    ? {
        themeToggleAria: 'Change theme',
        campusKyiv: 'Kyiv',
        campusMunich: 'Munich',
        trackBachelor: 'Bachelor',
        trackMaster: "Master's",
        trackTeacher: 'Teacher',
        level: 'Level',
        program: 'Program',
        chooseProgram: 'Choose a program',
        admissionYear: 'Admission year',
        campus: 'Campus',
        campusReadyTeacherHint: 'Choosing a campus automatically links the correct teacher course.',
        campusReadyStudentHint: 'The admission year defines the cohort. Choosing a campus automatically links the correct course.',
        chooseProgramFirstTitle: 'Choose a program first',
        chooseProgramFirstSubtitle: 'Available admission years will appear after that.',
        chooseProgramFirstTeacherSubtitle: 'For teachers, the campus is determined within the selected program.',
        noAdmissionYearsTitle: 'No admission years configured',
        noAdmissionYearsSubtitle: 'Without an admission year, Studerria cannot resolve the cohort and registration stops before subjects.',
        noTeacherPathTitle: 'No teacher pathway configured',
        noTeacherPathSubtitle: 'Without one active teacher path, Studerria cannot resolve the teacher workspace or subject flow for this program.',
        teacherPathRequiredHint: 'Teacher registration needs one active path for the selected program, otherwise the subject step cannot open correctly.',
        noAvailableAdmissionTitle: 'No admission year available',
        noAvailableAdmissionSubtitle: 'Ask an administrator to open one admission year, otherwise the student cohort cannot be resolved.',
        noCampusesTitle: 'No campuses configured',
        noCampusesTeacherSubtitle: 'Ask an administrator to link one teacher course per campus, otherwise the teacher route cannot be resolved.',
        noCampusesStudentSubtitle: 'Ask an administrator to link one visible course per campus, otherwise the student cannot move to subjects.',
        noCampusesTeacherHint: 'There are no campuses available for this teacher program yet.',
        noCampusesStudentHint: 'There are no campuses available for this admission year yet.',
        oneLinkedCourse: '1 linked course',
        linkedCoursesSuffix: 'linked courses',
        oneVisibleCourseNeeded: 'Exactly one visible course per campus is required, otherwise the student can land in the wrong subject flow.',
        readyToRegister: 'Ready: Studerria can resolve one stable course for this campus.',
        ambiguousCampusHint: 'Campus mapping is ambiguous. Ask an administrator to leave one visible course per campus, otherwise subject registration becomes unstable.',
        chooseValidCampus: 'Choose a valid campus before continuing, otherwise Studerria cannot resolve the correct course.',
      }
    : {
        themeToggleAria: 'Ð—Ð¼Ñ–Ð½Ð¸Ñ‚Ð¸ Ñ‚ÐµÐ¼Ñƒ',
        campusKyiv: 'ÐšÐ¸Ñ—Ð²',
        campusMunich: 'ÐœÑŽÐ½Ñ…ÐµÐ½',
        trackBachelor: 'Ð‘Ð°ÐºÐ°Ð»Ð°Ð²Ñ€Ð°Ñ‚',
        trackMaster: 'ÐœÐ°Ð³Ñ–ÑÑ‚Ñ€Ð°Ñ‚ÑƒÑ€Ð°',
        trackTeacher: 'Ð’Ð¸ÐºÐ»Ð°Ð´Ð°Ñ‡',
        level: 'Ð Ñ–Ð²ÐµÐ½ÑŒ',
        program: 'ÐŸÑ€Ð¾Ð³Ñ€Ð°Ð¼Ð°',
        chooseProgram: 'ÐžÐ±ÐµÑ€Ñ–Ñ‚ÑŒ Ð¿Ñ€Ð¾Ð³Ñ€Ð°Ð¼Ñƒ',
        admissionYear: 'Ð Ñ–Ðº Ð²ÑÑ‚ÑƒÐ¿Ñƒ',
        campus: 'ÐšÐ°Ð¼Ð¿ÑƒÑ',
        campusReadyTeacherHint: 'Ð’Ð¸Ð±Ñ–Ñ€ ÐºÐ°Ð¼Ð¿ÑƒÑÑƒ Ð°Ð²Ñ‚Ð¾Ð¼Ð°Ñ‚Ð¸Ñ‡Ð½Ð¾ Ð¿Ñ€Ð¸Ð²Ê¼ÑÐ·ÑƒÑ” Ð¿Ð¾Ñ‚Ñ€Ñ–Ð±Ð½Ð¸Ð¹ Ð²Ð¸ÐºÐ»Ð°Ð´Ð°Ñ†ÑŒÐºÐ¸Ð¹ ÐºÑƒÑ€Ñ.',
        campusReadyStudentHint: 'Ð Ñ–Ðº Ð²ÑÑ‚ÑƒÐ¿Ñƒ Ð²Ð¸Ð·Ð½Ð°Ñ‡Ð°Ñ” ÐºÐ¾Ð³Ð¾Ñ€Ñ‚Ñƒ. Ð’Ð¸Ð±Ñ–Ñ€ ÐºÐ°Ð¼Ð¿ÑƒÑÑƒ Ð°Ð²Ñ‚Ð¾Ð¼Ð°Ñ‚Ð¸Ñ‡Ð½Ð¾ Ð¿Ñ€Ð¸Ð²Ê¼ÑÐ·ÑƒÑ” Ð¿Ð¾Ñ‚Ñ€Ñ–Ð±Ð½Ð¸Ð¹ ÐºÑƒÑ€Ñ.',
        chooseProgramFirstTitle: 'Ð¡Ð¿ÐµÑ€ÑˆÑƒ Ð¾Ð±ÐµÑ€Ñ–Ñ‚ÑŒ Ð¿Ñ€Ð¾Ð³Ñ€Ð°Ð¼Ñƒ',
        chooseProgramFirstSubtitle: 'ÐŸÑ–ÑÐ»Ñ Ñ†ÑŒÐ¾Ð³Ð¾ Ð·Ê¼ÑÐ²Ð»ÑÑ‚ÑŒÑÑ Ð´Ð¾ÑÑ‚ÑƒÐ¿Ð½Ñ– Ñ€Ð¾ÐºÐ¸ Ð²ÑÑ‚ÑƒÐ¿Ñƒ.',
        chooseProgramFirstTeacherSubtitle: 'Ð”Ð»Ñ Ð²Ð¸ÐºÐ»Ð°Ð´Ð°Ñ‡Ð° ÐºÐ°Ð¼Ð¿ÑƒÑ Ð²Ð¸Ð·Ð½Ð°Ñ‡Ð°Ñ”Ñ‚ÑŒÑÑ Ð² Ð¼ÐµÐ¶Ð°Ñ… Ð¾Ð±Ñ€Ð°Ð½Ð¾Ñ— Ð¿Ñ€Ð¾Ð³Ñ€Ð°Ð¼Ð¸.',
        noAdmissionYearsTitle: 'ÐÐµÐ¼Ð°Ñ” Ð½Ð°Ð»Ð°ÑˆÑ‚Ð¾Ð²Ð°Ð½Ð¸Ñ… Ñ€Ð¾ÐºÑ–Ð² Ð²ÑÑ‚ÑƒÐ¿Ñƒ',
        noAdmissionYearsSubtitle: 'Áåç ðîêó âñòóïó Studerria íå âèçíà÷èòü êîãîðòó, òîìó ðåºñòðàö³ÿ çóïèíèòüñÿ ùå äî ïðåäìåò³â.',
        noTeacherPathTitle: 'ÐÐµÐ¼Ð°Ñ” Ð½Ð°Ð»Ð°ÑˆÑ‚Ð¾Ð²Ð°Ð½Ð¾Ð³Ð¾ Ð²Ð¸ÐºÐ»Ð°Ð´Ð°Ñ†ÑŒÐºÐ¾Ð³Ð¾ ÑˆÐ»ÑÑ…Ñƒ',
        noTeacherPathSubtitle: 'Áåç îäíîãî àêòèâíîãî teacher-path Studerria íå âèçíà÷èòü âèêëàäàöüêèé workspace ³ êîðåêòíèé subject flow.',
        teacherPathRequiredHint: 'Äëÿ ðåºñòðàö³¿ âèêëàäà÷à ïîòð³áåí îäèí àêòèâíèé øëÿõ, ³íàêøå êðîê ç ïðåäìåòàìè íå â³äêðèºòüñÿ êîðåêòíî.',
        noAvailableAdmissionTitle: 'ÐÐµÐ¼Ð°Ñ” Ð´Ð¾ÑÑ‚ÑƒÐ¿Ð½Ð¾Ð³Ð¾ Ñ€Ð¾ÐºÑƒ Ð²ÑÑ‚ÑƒÐ¿Ñƒ',
        noAvailableAdmissionSubtitle: 'Ïîïðîñ³òü àäì³í³ñòðàòîðà â³äêðèòè îäèí ð³ê âñòóïó, ³íàêøå êîãîðòà íå âèçíà÷èòüñÿ.',
        noCampusesTitle: 'ÐÐµÐ¼Ð°Ñ” Ð½Ð°Ð»Ð°ÑˆÑ‚Ð¾Ð²Ð°Ð½Ð¸Ñ… ÐºÐ°Ð¼Ð¿ÑƒÑÑ–Ð²',
        noCampusesTeacherSubtitle: 'Ïîïðîñ³òü àäì³í³ñòðàòîðà ïðèâ?ÿçàòè ïî îäíîìó teacher-course íà êàìïóñ, ³íàêøå ìàðøðóò íå âèçíà÷èòüñÿ.',
        noCampusesStudentSubtitle: 'Ïîïðîñ³òü àäì³í³ñòðàòîðà ïðèâ?ÿçàòè ïî îäíîìó âèäèìîìó êóðñó íà êàìïóñ, ³íàêøå ñòóäåíò íå ä³éäå äî ïðåäìåò³â.',
        noCampusesTeacherHint: 'Ð”Ð»Ñ Ñ†Ñ–Ñ”Ñ— Ð²Ð¸ÐºÐ»Ð°Ð´Ð°Ñ†ÑŒÐºÐ¾Ñ— Ð¿Ñ€Ð¾Ð³Ñ€Ð°Ð¼Ð¸ Ñ‰Ðµ Ð½ÐµÐ¼Ð°Ñ” Ð´Ð¾ÑÑ‚ÑƒÐ¿Ð½Ð¸Ñ… ÐºÐ°Ð¼Ð¿ÑƒÑÑ–Ð².',
        noCampusesStudentHint: 'Ð”Ð»Ñ Ñ†ÑŒÐ¾Ð³Ð¾ Ñ€Ð¾ÐºÑƒ Ð²ÑÑ‚ÑƒÐ¿Ñƒ Ñ‰Ðµ Ð½ÐµÐ¼Ð°Ñ” Ð´Ð¾ÑÑ‚ÑƒÐ¿Ð½Ð¸Ñ… ÐºÐ°Ð¼Ð¿ÑƒÑÑ–Ð².',
        oneLinkedCourse: '1 Ð¿Ñ€Ð¸Ð²Ê¼ÑÐ·Ð°Ð½Ð¸Ð¹ ÐºÑƒÑ€Ñ',
        linkedCoursesSuffix: 'Ð¿Ñ€Ð¸Ð²Ê¼ÑÐ·Ð°Ð½Ñ– ÐºÑƒÑ€ÑÐ¸',
        oneVisibleCourseNeeded: 'Ïîòð³áåí ð³âíî îäèí âèäèìèé êóðñ íà êàìïóñ, ³íàêøå ñòóäåíò ìîæå ïîòðàïèòè íå â òîé subject flow.',
        readyToRegister: 'Ãîòîâî: Studerria ìîæå âèçíà÷èòè îäèí ñòàá³ëüíèé êóðñ äëÿ öüîãî êàìïóñó.',
        ambiguousCampusHint: 'Ïðèâ?ÿçêà êàìïóñ³â íåîäíîçíà÷íà. Ïîïðîñ³òü àäì³í³ñòðàòîðà çàëèøèòè ïî îäíîìó âèäèìîìó êóðñó íà êàìïóñ, ³íàêøå subject registration áóäå íåñòàá³ëüíîþ.',
        chooseValidCampus: 'Îáåð³òü êîðåêòíèé êàìïóñ ïåðåä ïðîäîâæåííÿì, ³íàêøå Studerria íå âèçíà÷èòü ïîòð³áíèé êóðñ.',
      };
  const isTeacherCourse = (course) => course && (course.is_teacher_course === true || Number(course.is_teacher_course) === 1);
  const formatCampusLabel = (location) => String(location || 'kyiv').trim().toLowerCase() === 'munich'
    ? registerCourseText.campusMunich
    : registerCourseText.campusKyiv;
  const formatTrackLabel = (trackKey, fallback) => {
    const normalized = String(trackKey || '').trim().toLowerCase();
    if (normalized === 'bachelor') return registerCourseText.trackBachelor;
    if (normalized === 'master') return registerCourseText.trackMaster;
    if (normalized === 'teacher') return registerCourseText.trackTeacher;
    return fallback || normalized;
  };
%>
<!doctype html>
<html lang="<%= lang %>">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <%- include('partials/theme-bootstrap-head', { defaultTheme: 'light' }) %>
    <title><%= t('registerCourse.title') %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <style>
      :root {
        --accent: #3b82f6;
        --accent-dark: #2563eb;
        --card-radius: 20px;
        --glass-border-dark: rgba(255, 255, 255, 0.14);
        --glass-border-light: rgba(15, 23, 42, 0.08);
        --glass-bg-dark: rgba(255, 255, 255, 0.06);
        --glass-bg-light: rgba(255, 255, 255, 0.7);
      }
      body {
        font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", "SF Pro Display",
          "Helvetica Neue", "Inter", Arial, sans-serif;
        min-height: 100vh;
      }
      body.theme-light {
        background: radial-gradient(1200px 800px at 20% 10%, rgba(147, 197, 253, 0.35), transparent 60%),
          radial-gradient(1000px 700px at 90% 20%, rgba(196, 181, 253, 0.35), transparent 60%),
          linear-gradient(135deg, #eef2ff 0%, #f0f9ff 50%, #fdf4ff 100%);
        color: #0f172a;
      }
      body.theme-dark {
        background: radial-gradient(1200px 800px at 15% 10%, rgba(139, 92, 246, 0.3), transparent 60%),
          radial-gradient(1000px 700px at 80% 20%, rgba(59, 130, 246, 0.25), transparent 60%),
          linear-gradient(160deg, #050508 0%, #12061a 45%, #1b0f26 100%);
        color: #f8fafc;
      }
      .glass {
        position: relative;
        isolation: isolate;
        overflow: clip;
        background-clip: padding-box;
        border-radius: var(--card-radius);
        backdrop-filter: blur(20px);
        transition: background 220ms ease, border-color 220ms ease, box-shadow 220ms ease, transform 220ms ease;
      }
      body.theme-dark .glass {
        background: var(--glass-bg-dark);
        border: 1px solid var(--glass-border-dark);
        box-shadow: 0 18px 45px rgba(8, 8, 20, 0.55), 0 0 0 1px rgba(255, 255, 255, 0.03);
      }
      body.theme-light .glass {
        background: var(--glass-bg-light);
        border: 1px solid var(--glass-border-light);
        box-shadow: 0 16px 40px rgba(15, 23, 42, 0.1), 0 0 0 1px rgba(255, 255, 255, 0.7) inset;
      }
      body.page-register-course .auth-card {
        border-radius: 28px;
        border: 1px solid rgba(191, 219, 254, 0.16);
        overflow: hidden;
      }
      body.theme-dark.page-register-course .auth-card {
        background: linear-gradient(180deg, rgba(61, 70, 146, 0.9), rgba(44, 49, 126, 0.94)) !important;
        border-color: rgba(191, 219, 254, 0.18);
        box-shadow: 0 32px 72px rgba(5, 8, 24, 0.34), inset 0 1px 0 rgba(255, 255, 255, 0.14);
      }
      body.theme-light.page-register-course .auth-card {
        background: linear-gradient(180deg, rgba(255, 255, 255, 0.92), rgba(240, 245, 255, 0.88)) !important;
        border-color: rgba(148, 163, 184, 0.22);
        box-shadow: 0 26px 54px rgba(15, 23, 42, 0.12), inset 0 1px 0 rgba(255, 255, 255, 0.72);
      }
      body.page-register-course .auth-card .card-body {
        padding: 1.65rem 1.5rem !important;
      }
      body.theme-dark .auth-card h1 {
        color: #f8fafc;
      }
      body.page-register-course .auth-card h1 {
        font-weight: 700;
        letter-spacing: -0.03em;
        margin-bottom: 1rem !important;
      }
      .course-option {
        position: relative;
        border: 1px solid rgba(148, 163, 184, 0.45);
        border-radius: 14px;
        padding: 0.85rem 1rem;
        cursor: pointer;
        justify-content: space-between;
        gap: 0.85rem;
        transition: border-color 150ms ease, transform 150ms ease, box-shadow 150ms ease;
      }
      .course-option input {
        position: absolute;
        opacity: 0;
        pointer-events: none;
      }
      .course-option-content {
        display: flex;
        flex-direction: column;
        gap: 0.35rem;
        min-width: 0;
      }
      .campus-option .course-option-content {
        align-items: center;
        text-align: center;
      }
      .course-option-subtitle {
        font-size: 0.78rem;
        line-height: 1.35;
        opacity: 0.76;
      }
      .course-option-title {
        font-weight: 650;
        line-height: 1.26;
      }
      .course-option-meta {
        display: flex;
        flex-wrap: wrap;
        gap: 0.45rem;
      }
      .course-option-pill {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        min-height: 1.55rem;
        padding: 0.18rem 0.6rem;
        border-radius: 999px;
        font-size: 0.72rem;
        font-weight: 600;
        letter-spacing: -0.01em;
        border: 1px solid rgba(148, 163, 184, 0.28);
        background: rgba(255, 255, 255, 0.52);
        color: rgba(30, 41, 59, 0.78);
      }
      .course-option span {
        font-weight: 600;
      }
      .course-option:hover {
        transform: translateY(-1px);
        box-shadow: 0 12px 24px rgba(15, 23, 42, 0.12);
      }
      .course-option.is-disabled {
        opacity: 0.6;
        cursor: not-allowed;
        transform: none;
        box-shadow: none;
      }
      .course-option.is-selected {
        border-color: rgba(59, 130, 246, 0.9);
        background: rgba(59, 130, 246, 0.12);
        box-shadow: 0 10px 22px rgba(59, 130, 246, 0.2);
      }
      .campus-option {
        min-height: 78px;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 0.75rem 0.85rem;
      }
      .campus-option .course-option-title {
        font-size: 0.98rem;
      }
      .campus-option .course-option-subtitle {
        font-size: 0.74rem;
        max-width: 20ch;
        line-height: 1.34;
      }
      .admission-option {
        min-height: 78px;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 0.75rem 0.85rem;
      }
      .admission-option .course-option-content {
        align-items: center;
        text-align: center;
      }
      .admission-option .course-option-title {
        font-size: 0.98rem;
      }
      .admission-option .course-option-subtitle {
        font-size: 0.74rem;
        line-height: 1.34;
      }
      body.theme-dark .course-option {
        border-color: rgba(255, 255, 255, 0.15);
        background: rgba(255, 255, 255, 0.04);
      }
      body.theme-dark .course-option span {
        color: #f8fafc;
      }
      body.theme-dark .course-option .course-option-pill {
        border-color: rgba(191, 219, 254, 0.18);
        background: rgba(15, 23, 42, 0.34);
        color: rgba(226, 232, 240, 0.88);
      }
      body.theme-dark .course-option.is-selected {
        background: rgba(59, 130, 246, 0.2);
        border-color: rgba(59, 130, 246, 0.9);
      }
      body.page-register-course .course-option.is-selected .course-option-pill {
        border-color: rgba(99, 102, 241, 0.34);
        background: rgba(255, 255, 255, 0.22);
      }
      .pathway-grid {
        display: grid;
        gap: 12px;
        grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
      }
      .track-option {
        position: relative;
        border: 1px solid rgba(148, 163, 184, 0.45);
        border-radius: 14px;
        padding: 0.65rem 0.8rem;
        cursor: pointer;
        text-align: center;
        font-weight: 600;
        transition: border-color 150ms ease, transform 150ms ease, box-shadow 150ms ease, background 150ms ease;
      }
      .track-option input {
        position: absolute;
        opacity: 0;
        pointer-events: none;
      }
      .track-option:hover {
        transform: translateY(-1px);
        box-shadow: 0 10px 18px rgba(15, 23, 42, 0.1);
      }
      .track-option.is-selected {
        border-color: rgba(59, 130, 246, 0.9);
        background: rgba(59, 130, 246, 0.14);
        box-shadow: 0 10px 22px rgba(59, 130, 246, 0.2);
      }
      .track-option.is-disabled {
        opacity: 0.45;
        cursor: not-allowed;
      }
      .pathway-label {
        display: block;
        font-size: 0.85rem;
        opacity: 1;
        margin-bottom: 6px;
        font-weight: 600;
        letter-spacing: -0.01em;
      }
      body.theme-light.page-register-course .pathway-label {
        color: rgba(51, 65, 85, 0.78);
      }
      body.theme-dark.page-register-course .pathway-label {
        color: rgba(226, 232, 240, 0.86);
      }
      body.theme-dark .track-option {
        border-color: rgba(255, 255, 255, 0.2);
        background: rgba(255, 255, 255, 0.05);
      }
      body.theme-dark .track-option.is-selected {
        border-color: rgba(59, 130, 246, 0.9);
        background: rgba(59, 130, 246, 0.2);
      }
      .pathway-select {
        border-radius: 12px;
        border: 1px solid rgba(148, 163, 184, 0.42);
        min-height: 44px;
        font-weight: 600;
      }
      body.theme-dark .pathway-select {
        border-color: rgba(255, 255, 255, 0.2);
        background: rgba(255, 255, 255, 0.06);
        color: #f8fafc;
      }
      .pathway-hint {
        font-size: 0.8rem;
        opacity: 1;
        line-height: 1.45;
      }
      body.theme-light.page-register-course .pathway-hint {
        color: rgba(51, 65, 85, 0.72);
      }
      body.theme-dark.page-register-course .pathway-hint {
        color: rgba(226, 232, 240, 0.74);
      }
      body.page-register-course .track-option span,
      body.page-register-course .course-option .course-option-title {
        display: inline-block;
        color: inherit;
      }
      body.theme-light.page-register-course .track-option,
      body.theme-light.page-register-course .course-option {
        background: rgba(255, 255, 255, 0.42);
        border-color: rgba(148, 163, 184, 0.28);
        color: #0f172a;
      }
      body.theme-light.page-register-course .track-option.is-selected,
      body.theme-light.page-register-course .course-option.is-selected {
        background: linear-gradient(135deg, rgba(79, 70, 229, 0.18), rgba(168, 85, 247, 0.16));
        border-color: rgba(79, 70, 229, 0.46);
        box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
      }
      body.theme-dark.page-register-course .track-option,
      body.theme-dark.page-register-course .course-option {
        background: linear-gradient(180deg, rgba(255, 255, 255, 0.08), rgba(255, 255, 255, 0.05));
        border-color: rgba(191, 219, 254, 0.14);
        color: rgba(248, 250, 252, 0.94);
      }
      body.theme-dark.page-register-course .track-option:hover,
      body.theme-dark.page-register-course .course-option:hover {
        background: linear-gradient(180deg, rgba(255, 255, 255, 0.11), rgba(255, 255, 255, 0.07));
        box-shadow: 0 14px 28px rgba(8, 12, 28, 0.24);
      }
      body.theme-dark.page-register-course .track-option.is-selected,
      body.theme-dark.page-register-course .course-option.is-selected {
        background: linear-gradient(135deg, rgba(59, 130, 246, 0.24), rgba(139, 92, 246, 0.28));
        border-color: rgba(96, 165, 250, 0.72);
        box-shadow: 0 16px 32px rgba(37, 99, 235, 0.22);
      }
      body.theme-dark.page-register-course .track-option.is-selected span,
      body.theme-dark.page-register-course .course-option.is-selected span {
        color: #ffffff;
      }
      body.theme-light.page-register-course .pathway-select {
        background: rgba(255, 255, 255, 0.76);
        border-color: rgba(148, 163, 184, 0.26);
        color: #0f172a;
        box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
      }
      body.theme-dark.page-register-course .pathway-select {
        background: linear-gradient(180deg, rgba(255, 255, 255, 0.12), rgba(255, 255, 255, 0.08));
        border-color: rgba(191, 219, 254, 0.16);
        color: #f8fafc;
        box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.08), 0 12px 26px rgba(5, 8, 24, 0.18);
      }
      body.page-register-course .pathway-select option {
        color: #0f172a;
        background: #ffffff;
      }
      body.theme-dark.page-register-course .pathway-select option {
        color: #f8fafc;
        background: #1b2342;
      }
      body.page-register-course .register-action-btn {
        height: 46px;
        border-radius: 14px;
        font-weight: 600;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 0.75rem 1rem;
      }
      body.page-register-course .btn-primary.register-action-btn {
        background: #3b82f6;
        border: 1px solid rgba(59, 130, 246, 0.6);
      }
      body.page-register-course .btn-outline-primary.register-action-btn {
        border-radius: 14px;
      }
      .theme-toggle-floating {
        position: fixed;
        top: 16px;
        right: 16px;
        z-index: 10;
      }
      .theme-toggle-min {
        width: 40px;
        height: 40px;
        border-radius: 999px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        border: 1px solid rgba(255, 255, 255, 0.2);
        background: rgba(255, 255, 255, 0.08);
        color: inherit;
        backdrop-filter: blur(10px);
      }
      body.theme-light .theme-toggle-min {
        border-color: rgba(15, 23, 42, 0.12);
        background: rgba(255, 255, 255, 0.7);
      }
      .btn {
        transition: transform 140ms ease, box-shadow 140ms ease;
      }
      .btn:hover {
        transform: translateY(-1px);
        box-shadow: 0 10px 22px rgba(15, 23, 42, 0.12);
      }
      body.theme-dark .btn:hover {
        box-shadow: 0 10px 22px rgba(0, 0, 0, 0.4);
      }
      .btn:active {
        transform: translateY(0);
        box-shadow: none;
      }
      .app-footer {
        font-size: 0.8rem;
      }
      body.theme-dark .app-footer {
        color: rgba(255, 255, 255, 0.6);
      }
      body.theme-light .app-footer {
        color: rgba(15, 23, 42, 0.6);
      }
</style>
    <link rel="stylesheet" href="/css/tokens.css" />
    <link rel="stylesheet" href="/css/components.css" />
    <link rel="stylesheet" href="/css/pages/auth.css" />
    <link rel="stylesheet" href="/css/auth-actions.css" />
    <link rel="stylesheet" href="/css/design-system.css" />
    <link rel="stylesheet" href="/css/studerria-ui.css" />
    <link rel="stylesheet" href="/css/dynamic-bg.css" />
  </head>
  <body class="studerria-theme theme-light page-register-course">
    <%- include('partials/theme-bootstrap-body') %>
    <%- include('partials/dynamic-bg') %>
    <div class="theme-toggle-floating">
      <button
        class="theme-toggle-min"
        type="button"
        id="themeToggle"
        aria-label="<%= registerCourseText.themeToggleAria %>"
        data-light-label="â˜€ï¸"
        data-dark-label="ðŸŒ™"
      >
        ðŸŒ™
      </button>
    </div>
    <div class="container">
      <div class="row justify-content-center align-items-center" style="min-height: 100vh;">
        <div class="col-11 col-sm-8 col-md-6 col-lg-4">
          <div class="card auth-card glass">
            <div class="card-body p-4">
              <h1 class="h4 mb-3 text-center"><%= t('registerCourse.title') %></h1>
              <% if (error) { %>
              <div class="alert alert-danger" role="alert"><%= error %></div>
              <% } %>
              <form method="POST" action="/register/course">
                <div class="mb-3">
                  <span class="pathway-label"><%= registerCourseText.level %></span>
                  <div class="pathway-grid" id="trackGrid">
                    <% (pathways.tracks || []).forEach((track) => { %>
                      <label
                        class="track-option <%= track.enabled ? '' : 'is-disabled' %>"
                        data-track-card="<%= track.key %>"
                      >
                        <input
                          type="radio"
                          name="study_track"
                          value="<%= track.key %>"
                          <%= selectedTrackValue === track.key ? 'checked' : '' %>
                          <%= track.enabled ? '' : 'disabled' %>
                          required
                        />
                        <span><%= formatTrackLabel(track.key, track.label) %></span>
                      </label>
                    <% }) %>
                  </div>
                </div>

                <div class="mb-3" id="programField">
                  <label class="pathway-label" for="studyProgramSelect"><%= registerCourseText.program %></label>
                  <select
                    id="studyProgramSelect"
                    name="study_program_id"
                    class="form-select pathway-select"
                    data-selected-program="<%= selectedProgramValue || '' %>"
                  >
                    <option value=""><%= registerCourseText.chooseProgram %></option>
                  </select>
                </div>
                <div class="mb-3" id="programStaticField" style="display: none;">
                  <span class="pathway-label"><%= registerCourseText.program %></span>
                  <div class="track-option is-selected">
                    <span id="programStaticLabel"></span>
                  </div>
                </div>

                <div class="mb-3" id="admissionField">
                  <label class="pathway-label"><%= registerCourseText.admissionYear %></label>
                  <input type="hidden" name="admission_id" id="selectedAdmissionInput" value="<%= selectedAdmissionValue || '' %>" />
                  <div class="pathway-grid" id="admissionOptions"></div>
                </div>

                <div class="mb-3">
                  <label class="pathway-label"><%= registerCourseText.campus %></label>
                  <input type="hidden" name="course_id" id="selectedCourseIdInput" value="<%= Number(selectedCourseId || 0) || '' %>" />
                  <input type="hidden" name="campus" id="selectedCampusInput" value="<%= selectedCampusValue %>" />
                  <div class="pathway-grid" id="campusOptions"></div>
                </div>
                <p class="pathway-hint mb-3" id="campusHint"><%= registerCourseText.campusReadyStudentHint %></p>
                <div class="auth-action-stack auth-action-stack--grid">
                  <button type="submit" class="btn btn-primary register-action-btn auth-action-btn"><%= t('registerCourse.continue') %></button>
                  <a href="/register" class="btn btn-outline-primary register-action-btn auth-action-btn"><%= t('registerCourse.backToRegister') %></a>
                </div>
              </form>
            </div>
          </div>
          <%- include('partials/app-footer') %>
        </div>
      </div>
    </div>
    <%- include('partials/changelog-modal') %>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
    <script>
      const isEnglishRegisterCourse = <%- JSON.stringify(lang === 'en') %>;
      const registerCourseText = <%- JSON.stringify(registerCourseText) %>;
      const pathwayPayload = <%- JSON.stringify(pathways) %>;
      const pathwayPrograms = Array.isArray(pathwayPayload.programs) ? pathwayPayload.programs : [];
      const pathwayAdmissions = Array.isArray(pathwayPayload.admissions) ? pathwayPayload.admissions : [];
      const pathwayLinks = Array.isArray(pathwayPayload.links) ? pathwayPayload.links : [];
      const pathwayCourses = Array.isArray(pathwayPayload.courses) ? pathwayPayload.courses : [];
      const trackInputs = Array.from(document.querySelectorAll('input[name="study_track"]'));
      const programSelect = document.getElementById('studyProgramSelect');
      const programField = document.getElementById('programField');
      const programStaticField = document.getElementById('programStaticField');
      const programStaticLabel = document.getElementById('programStaticLabel');
      const admissionField = document.getElementById('admissionField');
      const admissionInput = document.getElementById('selectedAdmissionInput');
      const admissionOptions = document.getElementById('admissionOptions');
      const courseIdInput = document.getElementById('selectedCourseIdInput');
      const campusInput = document.getElementById('selectedCampusInput');
      const campusOptions = document.getElementById('campusOptions');
      const campusHint = document.getElementById('campusHint');
      const registerCourseForm = document.querySelector('form[action="/register/course"]');
      const selectedProgramSeed = Number(programSelect?.dataset.selectedProgram || 0);
      const selectedAdmissionSeed = Number(admissionInput?.value || 0);
      const selectedCourseSeed = Number(courseIdInput?.value || 0);
      const selectedCampusSeed = String(campusInput?.value || '').trim().toLowerCase();
      const courseById = new Map(
        pathwayCourses.map((course) => [Number(course.id || 0), course])
      );

      function formatLinkedCourseCount(count) {
        return Number(count || 0) === 1
          ? registerCourseText.oneLinkedCourse
          : `${count} ${registerCourseText.linkedCoursesSuffix}`;
      }

      function getSelectedTrack() {
        const checked = trackInputs.find((input) => input.checked);
        if (checked) return checked.value;
        const firstEnabled = trackInputs.find((input) => !input.disabled);
        if (firstEnabled) {
          firstEnabled.checked = true;
          return firstEnabled.value;
        }
        return 'bachelor';
      }

      function isTeacherCourseClient(course) {
        return Boolean(course) && (
          course.is_teacher_course === true
          || Number(course.is_teacher_course) === 1
        );
      }

      function normalizeCampusKey(rawValue) {
        return String(rawValue || '').trim().toLowerCase() === 'munich' ? 'munich' : 'kyiv';
      }

      function formatCampusLabelClient(location) {
        return normalizeCampusKey(location) === 'munich'
          ? registerCourseText.campusMunich
          : registerCourseText.campusKyiv;
      }

      function localizeAdmissionLabel(rawLabel) {
        const normalized = String(rawLabel || '').trim();
        if (isEnglishRegisterCourse) return normalized;
        return normalized
          .replace(/^Cohort\b/i, 'ÐšÐ¾Ð³Ð¾Ñ€Ñ‚Ð°')
          .replace(/\bCohort\b/gi, 'ÐšÐ¾Ð³Ð¾Ñ€Ñ‚Ð°');
      }

      function isTeacherTrack(trackKey) {
        return String(trackKey || '').trim().toLowerCase() === 'teacher';
      }

      function getCampusReadyHint(trackKey) {
        return isTeacherTrack(trackKey)
          ? registerCourseText.campusReadyTeacherHint
          : registerCourseText.campusReadyStudentHint;
      }

      function syncTrackCards() {
        document.querySelectorAll('[data-track-card]').forEach((card) => {
          const key = card.dataset.trackCard;
          const input = card.querySelector(`input[name="study_track"][value="${key}"]`);
          card.classList.toggle('is-selected', Boolean(input && input.checked));
          card.classList.toggle('is-disabled', Boolean(input && input.disabled));
        });
      }

      function setOptions(selectEl, options, selectedValue, emptyLabel) {
        if (!selectEl) return;
        selectEl.innerHTML = '';
        const emptyOption = document.createElement('option');
        emptyOption.value = '';
        emptyOption.textContent = emptyLabel;
        selectEl.appendChild(emptyOption);
        options.forEach((option) => {
          const node = document.createElement('option');
          node.value = String(option.value);
          node.textContent = option.label;
          if (String(selectedValue || '') === String(option.value)) {
            node.selected = true;
          }
          selectEl.appendChild(node);
        });
      }

      function refreshProgramOptions() {
        const trackKey = getSelectedTrack();
        const options = pathwayPrograms
          .filter((program) => program.track_key === trackKey)
          .map((program) => ({
            value: Number(program.id),
            label: program.code ? `${program.name} (${program.code})` : program.name,
          }));
        const currentValue = Number(programSelect?.value || selectedProgramSeed || 0);
        const resolvedValue = options.some((option) => Number(option.value) === currentValue)
          ? currentValue
          : (options[0] ? Number(options[0].value) : null);
        setOptions(programSelect, options, resolvedValue, registerCourseText.chooseProgram);
        if (programField && programStaticField && programStaticLabel) {
          if (options.length === 1) {
            programField.style.display = 'none';
            programStaticField.style.display = '';
            programStaticLabel.textContent = options[0].label;
          } else {
            programField.style.display = '';
            programStaticField.style.display = 'none';
            programStaticLabel.textContent = '';
          }
        }
      }

      function refreshAdmissionOptions() {
        const trackKey = getSelectedTrack();
        const programId = Number(programSelect?.value || 0);
        const options = pathwayAdmissions
          .filter((admission) => Number(admission.program_id) === programId)
          .map((admission) => ({
            value: Number(admission.id),
            title: String(admission.admission_year || registerCourseText.admissionYear),
            subtitle: localizeAdmissionLabel(admission.label),
          }));
        const currentValue = Number(admissionInput?.value || selectedAdmissionSeed || 0);
        const resolvedValue = options.some((option) => Number(option.value) === currentValue)
          ? currentValue
          : (options[0] ? Number(options[0].value) : null);
        if (admissionInput) {
          admissionInput.value = resolvedValue ? String(resolvedValue) : '';
        }
        if (admissionField) {
          admissionField.style.display = isTeacherTrack(trackKey) ? 'none' : '';
        }
        if (!admissionOptions) return;
        admissionOptions.innerHTML = '';
        if (isTeacherTrack(trackKey)) {
          return;
        }
        if (!programId) {
          const emptyState = document.createElement('div');
          emptyState.className = 'track-option admission-option is-disabled';
          emptyState.innerHTML = `<span class="course-option-content"><span class="course-option-title">${registerCourseText.chooseProgramFirstTitle}</span><span class="course-option-subtitle">${registerCourseText.chooseProgramFirstSubtitle}</span></span>`;
          admissionOptions.appendChild(emptyState);
          return;
        }
        if (!options.length) {
          const emptyState = document.createElement('div');
          emptyState.className = 'track-option admission-option is-disabled';
          emptyState.innerHTML = `<span class="course-option-content"><span class="course-option-title">${registerCourseText.noAdmissionYearsTitle}</span><span class="course-option-subtitle">${registerCourseText.noAdmissionYearsSubtitle}</span></span>`;
          admissionOptions.appendChild(emptyState);
          return;
        }

        options.forEach((option) => {
          const button = document.createElement('button');
          button.type = 'button';
          button.className = 'course-option admission-option';
          if (Number(option.value) === Number(resolvedValue)) {
            button.classList.add('is-selected');
          }
          button.innerHTML = `
            <span class="course-option-content">
              <span class="course-option-title">${option.title}</span>
              ${option.subtitle ? `<span class="course-option-subtitle">${option.subtitle}</span>` : ''}
            </span>
          `;
          button.addEventListener('click', () => {
            admissionOptions.querySelectorAll('.admission-option').forEach((item) => item.classList.remove('is-selected'));
            button.classList.add('is-selected');
            if (admissionInput) {
              admissionInput.value = String(option.value);
            }
            renderCampusOptions();
          });
          admissionOptions.appendChild(button);
        });
      }

      function getAdmissionCourses(admissionId, trackKey) {
        const targetAdmissionId = Number(admissionId || 0);
        if (!targetAdmissionId) return [];
        const courseIds = [];
        pathwayLinks.forEach((link) => {
          const linkAdmissionId = Number(link.admission_id || 0);
          const courseId = Number(link.course_id || 0);
          if (linkAdmissionId !== targetAdmissionId || !courseId) return;
          if (!courseById.has(courseId)) return;
          if (!courseIds.includes(courseId)) {
            courseIds.push(courseId);
          }
        });

        return courseIds
          .map((courseId) => courseById.get(courseId))
          .filter((course) => {
            const teacherCourse = isTeacherCourseClient(course);
            return trackKey === 'teacher' ? teacherCourse : !teacherCourse;
          });
      }

      function buildCampusChoices(admissionId, trackKey) {
        const campusMap = new Map();
        getAdmissionCourses(admissionId, trackKey).forEach((course) => {
          const courseId = Number(course.id || 0);
          if (!courseId) return;
          const campusKey = normalizeCampusKey(course.location);
          if (!campusMap.has(campusKey)) {
            campusMap.set(campusKey, {
              campusKey,
              campusLabel: formatCampusLabelClient(campusKey),
              courseIds: [],
            });
          }
          campusMap.get(campusKey).courseIds.push(courseId);
        });

        return ['kyiv', 'munich']
          .map((campusKey) => campusMap.get(campusKey))
          .filter(Boolean)
          .map((entry) => {
            const uniqueIds = Array.from(new Set(entry.courseIds));
            return {
              campusKey: entry.campusKey,
              campusLabel: entry.campusLabel,
              courseCount: uniqueIds.length,
              courseId: uniqueIds.length === 1 ? uniqueIds[0] : null,
              isAmbiguous: uniqueIds.length !== 1,
            };
          });
      }

      function setCampusHint(message, isError = false) {
        if (!campusHint) return;
        campusHint.textContent = message;
        campusHint.style.color = isError ? '#dc2626' : '';
      }

      function renderCampusOptions() {
        if (!campusOptions || !courseIdInput || !campusInput) return;
        const trackKey = getSelectedTrack();
        const programId = Number(programSelect?.value || 0);
        const admissionId = Number(admissionInput?.value || 0);
        const campusChoices = buildCampusChoices(admissionId, trackKey);
        const preferredCourse = courseById.get(selectedCourseSeed);
        const preferredCampus = selectedCampusSeed || (preferredCourse ? normalizeCampusKey(preferredCourse.location) : '');
        const activeCourseId = Number(courseIdInput.value || 0);
        const activeCampusKey = String(campusInput.value || preferredCampus || '').trim().toLowerCase();

        campusOptions.innerHTML = '';
        courseIdInput.value = '';
        campusInput.value = '';

        if (!admissionId) {
          const emptyState = document.createElement('div');
          emptyState.className = 'track-option campus-option is-disabled';
          if (isTeacherTrack(trackKey)) {
            if (!programId) {
              emptyState.innerHTML = `<span class="course-option-content"><span class="course-option-title">${registerCourseText.chooseProgramFirstTitle}</span><span class="course-option-subtitle">${registerCourseText.chooseProgramFirstTeacherSubtitle}</span></span>`;
              campusOptions.appendChild(emptyState);
              setCampusHint(registerCourseText.campusReadyTeacherHint, true);
              return;
            }
            emptyState.innerHTML = `<span class="course-option-content"><span class="course-option-title">${registerCourseText.noTeacherPathTitle}</span><span class="course-option-subtitle">${registerCourseText.noTeacherPathSubtitle}</span></span>`;
            campusOptions.appendChild(emptyState);
            setCampusHint(registerCourseText.teacherPathRequiredHint, true);
            return;
          }
          if (!programId) {
            emptyState.innerHTML = `<span class="course-option-content"><span class="course-option-title">${registerCourseText.chooseProgramFirstTitle}</span><span class="course-option-subtitle">${registerCourseText.chooseProgramFirstSubtitle}</span></span>`;
          } else {
            emptyState.innerHTML = `<span class="course-option-content"><span class="course-option-title">${registerCourseText.noAvailableAdmissionTitle}</span><span class="course-option-subtitle">${registerCourseText.noAvailableAdmissionSubtitle}</span></span>`;
          }
          campusOptions.appendChild(emptyState);
          setCampusHint(getCampusReadyHint(trackKey));
          return;
        }

        if (!campusChoices.length) {
          const emptyState = document.createElement('div');
          emptyState.className = 'track-option campus-option is-disabled';
          emptyState.innerHTML = `<span class="course-option-content"><span class="course-option-title">${registerCourseText.noCampusesTitle}</span><span class="course-option-subtitle">${isTeacherTrack(trackKey) ? registerCourseText.noCampusesTeacherSubtitle : registerCourseText.noCampusesStudentSubtitle}</span></span>`;
          campusOptions.appendChild(emptyState);
          setCampusHint(
            isTeacherTrack(trackKey)
              ? registerCourseText.noCampusesTeacherHint
              : registerCourseText.noCampusesStudentHint,
            true
          );
          return;
        }

        let selectedChoice = campusChoices.find((choice) => choice.courseId === activeCourseId) || null;
        if (!selectedChoice && activeCampusKey) {
          selectedChoice = campusChoices.find((choice) => choice.campusKey === activeCampusKey && !choice.isAmbiguous) || null;
        }
        if (!selectedChoice && preferredCampus) {
          selectedChoice = campusChoices.find((choice) => choice.campusKey === preferredCampus && !choice.isAmbiguous) || null;
        }
        if (!selectedChoice) {
          selectedChoice = campusChoices.find((choice) => !choice.isAmbiguous) || null;
        }

        campusChoices.forEach((choice) => {
          const button = document.createElement('button');
          button.type = 'button';
          button.className = `track-option campus-option ${choice.isAmbiguous ? 'is-disabled' : ''}`;
          if (selectedChoice && selectedChoice.campusKey === choice.campusKey && !choice.isAmbiguous) {
            button.classList.add('is-selected');
            courseIdInput.value = String(choice.courseId || '');
            campusInput.value = choice.campusKey;
          }

          const courseCountLabel = formatLinkedCourseCount(choice.courseCount);
          const subtitle = choice.isAmbiguous
            ? `${courseCountLabel}. ${registerCourseText.oneVisibleCourseNeeded}`
            : `${courseCountLabel}. ${registerCourseText.readyToRegister}`;
          button.innerHTML = `
            <span class="course-option-content">
              <span class="course-option-title">${choice.campusLabel}</span>
              <span class="course-option-subtitle">${subtitle}</span>
            </span>
          `;

          if (!choice.isAmbiguous) {
            button.addEventListener('click', () => {
              campusOptions.querySelectorAll('.campus-option').forEach((item) => item.classList.remove('is-selected'));
              button.classList.add('is-selected');
              courseIdInput.value = String(choice.courseId || '');
              campusInput.value = choice.campusKey;
              setCampusHint(getCampusReadyHint(trackKey));
            });
          }

          campusOptions.appendChild(button);
        });

        if (!courseIdInput.value || !campusInput.value) {
          setCampusHint(registerCourseText.ambiguousCampusHint, true);
        } else {
          setCampusHint(getCampusReadyHint(trackKey));
        }
      }

      function refreshPathwaySelections() {
        syncTrackCards();
        refreshProgramOptions();
        refreshAdmissionOptions();
        renderCampusOptions();
      }

      trackInputs.forEach((input) => {
        input.addEventListener('change', refreshPathwaySelections);
      });

      if (programSelect) {
        programSelect.addEventListener('change', () => {
          refreshAdmissionOptions();
          renderCampusOptions();
        });
      }

      if (registerCourseForm) {
        registerCourseForm.addEventListener('submit', (event) => {
          if (courseIdInput && !courseIdInput.value) {
            event.preventDefault();
            setCampusHint(registerCourseText.chooseValidCampus, true);
          }
        });
      }

      refreshPathwaySelections();

      const themeToggle = document.getElementById('themeToggle');
      const savedTheme = localStorage.getItem('ui-theme') || 'theme-light';
      document.body.classList.remove('theme-light', 'theme-dark');
      document.body.classList.add(savedTheme);
      themeToggle.textContent = savedTheme === 'theme-dark' ? themeToggle.dataset.lightLabel : themeToggle.dataset.darkLabel;
      themeToggle.addEventListener('click', () => {
        const next = document.body.classList.contains('theme-dark') ? 'theme-light' : 'theme-dark';
        document.body.classList.remove('theme-light', 'theme-dark');
        document.body.classList.add(next);
        localStorage.setItem('ui-theme', next);
        themeToggle.textContent = next === 'theme-dark' ? themeToggle.dataset.lightLabel : themeToggle.dataset.darkLabel;
      });
    </script>
      <script src="/js/design-system.js" defer></script>
      <script src="/js/dynamic-bg.js" defer></script>
  </body>
</html>
