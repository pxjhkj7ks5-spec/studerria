<%
  const scopeMatrix = registrationScopeMatrix || {
    tracks: [],
    programs: [],
    cohortsByProgram: {},
    campusesByCohort: {},
    teacherCampusesByProgram: {},
    initialSelection: {},
  };
  const initialSelection = scopeMatrix.initialSelection || {};
  const selectedTrackValue = typeof initialSelection.trackKey === 'string' && initialSelection.trackKey
    ? initialSelection.trackKey
    : (typeof selectedTrack === 'string' && selectedTrack ? selectedTrack : 'bachelor');
  const formatTrackLabel = (trackKey, fallback) => {
    const normalized = String(trackKey || '').trim().toLowerCase();
    if (normalized === 'bachelor') return lang === 'en' ? 'Bachelor' : 'Бакалаврат';
    if (normalized === 'master') return lang === 'en' ? "Master's" : 'Магістратура';
    if (normalized === 'teacher') return lang === 'en' ? 'Teacher' : 'Викладач';
    return fallback || normalized;
  };
  const registerCourseText = lang === 'en'
    ? {
        themeToggleAria: 'Change theme',
        introLabel: 'Academic scope',
        introTitle: 'Choose the registration route',
        introSubtitle: 'Studerria uses one explicit academic scope for the next registration step. Cohort and campus stay visible and never auto-confirm silently.',
        stepTrack: 'Level',
        stepTrackCopy: 'Choose the route you register into. The next steps will rebuild around this academic level.',
        stepProgram: 'Program',
        stepProgramCopy: 'Program sets the working context. Cohort and campus remain a separate explicit choice.',
        stepCohort: 'Cohort',
        stepCohortCopy: 'Pick the cohort you want to join. Blocked cohorts stay visible with setup reasons.',
        stepCampus: 'Campus',
        stepCampusCopyStudent: 'After the cohort is confirmed, choose the campus that resolves the exact academic group.',
        stepCampusCopyTeacher: 'Choose the campus that resolves the teacher academic group for this program.',
        chooseProgram: 'Choose a program',
        chooseProgramFirst: 'Choose a program first to unlock cohort choices.',
        noPrograms: 'No programs available for this route yet.',
        noCohorts: 'No cohort is available for this program yet.',
        noReadyCohorts: 'This program has no ready cohort for registration yet.',
        chooseCohortFirst: 'Choose a cohort to review available campuses.',
        noCampuses: 'No campuses are available for this scope yet.',
        noReadyCampuses: 'This scope has no ready campus for registration yet.',
        chooseCampus: 'Choose a campus to confirm the academic scope.',
        summaryLabel: 'Confirmed academic scope',
        summaryEmptyStudent: 'Choose program, cohort, and campus to continue.',
        summaryEmptyTeacher: 'Choose program and campus to continue.',
        summaryPending: 'Selection in progress',
        summaryReady: 'Scope confirmed',
        ready: 'Ready',
        blocked: 'Blocked',
        needsSetup: 'Needs setup',
        cohortLabel: 'Cohort',
        admissionYearLabel: 'Admission year',
        campusLabel: 'Campus',
        campusKyiv: 'Kyiv',
        campusMunich: 'Munich',
        availableCampusesOne: '1 campus ready',
        availableCampusesMany: 'campuses ready',
        totalCampusesLabel: 'campuses total',
        scopeProgram: 'Program',
        scopeCohort: 'Cohort',
        scopeRoute: 'Route',
        scopeGroup: 'Academic group',
        continueLockedStudent: 'Choose cohort and campus before continuing.',
        continueLockedTeacher: 'Choose campus before continuing.',
      }
    : {
        themeToggleAria: 'Змінити тему',
        introLabel: 'Академічний контекст',
        introTitle: 'Оберіть маршрут реєстрації',
        introSubtitle: 'Studerria продовжує реєстрацію лише в межах одного явного академічного контексту. Когорта й кампус залишаються видимими та не підтверджуються автоматично.',
        stepTrack: 'Рівень',
        stepTrackCopy: 'Оберіть маршрут, у який ви реєструєтесь. Далі сторінка перебудується під цей академічний рівень.',
        stepProgram: 'Програма',
        stepProgramCopy: 'Програма задає робочий контекст. Когорта і кампус нижче залишаються окремим явним вибором.',
        stepCohort: 'Когорта',
        stepCohortCopy: 'Оберіть когорту, до якої хочете приєднатись. Заблоковані когорти залишаються видимими з причиною.',
        stepCampus: 'Кампус',
        stepCampusCopyStudent: 'Після підтвердження когорти оберіть кампус, який привʼязує точну академічну групу.',
        stepCampusCopyTeacher: 'Оберіть кампус, який привʼязує викладацьку академічну групу для цієї програми.',
        chooseProgram: 'Оберіть програму',
        chooseProgramFirst: 'Спершу оберіть програму, щоб побачити когорти.',
        noPrograms: 'Для цього маршруту ще немає доступних програм.',
        noCohorts: 'Для цієї програми поки немає жодної когорти.',
        noReadyCohorts: 'Для цієї програми поки немає готової когорти для реєстрації.',
        chooseCohortFirst: 'Оберіть когорту, щоб переглянути доступні кампуси.',
        noCampuses: 'Для цього контексту поки немає доступних кампусів.',
        noReadyCampuses: 'Для цього контексту поки немає готового кампусу для реєстрації.',
        chooseCampus: 'Оберіть кампус, щоб підтвердити академічний маршрут.',
        summaryLabel: 'Підтверджений академічний контекст',
        summaryEmptyStudent: 'Оберіть програму, когорту й кампус, щоб продовжити.',
        summaryEmptyTeacher: 'Оберіть програму й кампус, щоб продовжити.',
        summaryPending: 'Вибір ще не завершено',
        summaryReady: 'Контекст підтверджено',
        ready: 'Готово',
        blocked: 'Заблоковано',
        needsSetup: 'Потрібне налаштування',
        cohortLabel: 'Когорта',
        admissionYearLabel: 'Рік вступу',
        campusLabel: 'Кампус',
        campusKyiv: 'Київ',
        campusMunich: 'Мюнхен',
        availableCampusesOne: '1 кампус готовий',
        availableCampusesMany: 'кампусів готово',
        totalCampusesLabel: 'кампусів у маршруті',
        scopeProgram: 'Програма',
        scopeCohort: 'Когорта',
        scopeRoute: 'Маршрут',
        scopeGroup: 'Академічна група',
        continueLockedStudent: 'Оберіть когорту і кампус перед продовженням.',
        continueLockedTeacher: 'Оберіть кампус перед продовженням.',
      };
%>
<!doctype html>
<html lang="<%= lang %>">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <%- include('partials/theme-bootstrap-head', { defaultTheme: 'light' }) %>
    <title><%= t('registerCourse.title') %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <style>
      :root {
        --accent: #2583ff;
        --accent-strong: #1068de;
        --accent-soft: rgba(37, 131, 255, 0.14);
        --success: #118a56;
        --warning: #b86b00;
        --danger: #cf3c44;
        --card-radius: 28px;
      }
      body {
        font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", "SF Pro Display", "Helvetica Neue", Arial, sans-serif;
        min-height: 100vh;
      }
      body.theme-light {
        background:
          radial-gradient(1200px 900px at 10% 10%, rgba(96, 165, 250, 0.28), transparent 60%),
          radial-gradient(1000px 800px at 92% 12%, rgba(34, 211, 238, 0.2), transparent 58%),
          linear-gradient(160deg, #eef6ff 0%, #f7fbff 54%, #f6f7fb 100%);
        color: #0f172a;
      }
      body.theme-dark {
        background:
          radial-gradient(1200px 900px at 8% 10%, rgba(32, 124, 229, 0.28), transparent 62%),
          radial-gradient(1000px 780px at 88% 10%, rgba(14, 165, 233, 0.16), transparent 60%),
          linear-gradient(180deg, #07111d 0%, #0a1727 48%, #0d1d31 100%);
        color: #f8fafc;
      }
      .glass {
        position: relative;
        isolation: isolate;
        overflow: clip;
        background-clip: padding-box;
        border-radius: var(--card-radius);
        backdrop-filter: blur(22px) saturate(160%);
        -webkit-backdrop-filter: blur(22px) saturate(160%);
        transition: border-color 180ms ease, box-shadow 180ms ease, background 180ms ease, transform 180ms ease;
      }
      body.theme-light .glass {
        background: linear-gradient(180deg, rgba(255, 255, 255, 0.86), rgba(244, 249, 255, 0.78));
        border: 1px solid rgba(148, 163, 184, 0.16);
        box-shadow: 0 28px 64px rgba(15, 23, 42, 0.12), inset 0 1px 0 rgba(255, 255, 255, 0.85);
      }
      body.theme-dark .glass {
        background: linear-gradient(180deg, rgba(16, 29, 46, 0.78), rgba(13, 25, 42, 0.72));
        border: 1px solid rgba(148, 191, 255, 0.14);
        box-shadow: 0 34px 72px rgba(1, 8, 20, 0.42), inset 0 1px 0 rgba(255, 255, 255, 0.08);
      }
      body.page-register-course .auth-card {
        border-radius: 34px;
        overflow: hidden;
      }
      body.page-register-course .auth-card .card-body {
        padding: 1.8rem 1.6rem !important;
      }
      body.page-register-course .auth-card h1 {
        margin: 0;
        font-size: clamp(1.45rem, 3vw, 1.95rem);
        line-height: 1.06;
        letter-spacing: -0.04em;
        font-weight: 760;
      }
      .registration-intro {
        margin-bottom: 1.15rem;
        padding: 1.1rem 1.15rem 1.05rem;
        border-radius: 24px;
        border: 1px solid rgba(148, 163, 184, 0.14);
      }
      body.theme-light .registration-intro {
        background: linear-gradient(135deg, rgba(255, 255, 255, 0.72), rgba(238, 247, 255, 0.78));
      }
      body.theme-dark .registration-intro {
        background: linear-gradient(135deg, rgba(17, 32, 53, 0.74), rgba(10, 24, 41, 0.78));
      }
      .registration-eyebrow {
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        min-height: 1.8rem;
        padding: 0.2rem 0.72rem;
        border-radius: 999px;
        font-size: 0.74rem;
        font-weight: 700;
        letter-spacing: 0.01em;
        text-transform: uppercase;
      }
      body.theme-light .registration-eyebrow {
        background: rgba(37, 131, 255, 0.1);
        color: rgba(17, 76, 149, 0.9);
      }
      body.theme-dark .registration-eyebrow {
        background: rgba(96, 165, 250, 0.12);
        color: rgba(191, 219, 254, 0.92);
      }
      .registration-intro-copy {
        margin-top: 0.8rem;
      }
      .registration-intro-copy p {
        margin: 0.5rem 0 0;
        font-size: 0.92rem;
        line-height: 1.55;
      }
      body.theme-light .registration-intro-copy p {
        color: rgba(51, 65, 85, 0.84);
      }
      body.theme-dark .registration-intro-copy p {
        color: rgba(226, 232, 240, 0.8);
      }
      .registration-step {
        padding: 1rem 0 1.05rem;
        border-top: 1px solid rgba(148, 163, 184, 0.14);
      }
      .registration-step:first-of-type {
        border-top: 0;
        padding-top: 0.25rem;
      }
      .registration-step-header {
        display: flex;
        align-items: flex-start;
        gap: 0.85rem;
        margin-bottom: 0.8rem;
      }
      .registration-step-index {
        flex: 0 0 auto;
        width: 1.9rem;
        height: 1.9rem;
        border-radius: 999px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        font-size: 0.82rem;
        font-weight: 700;
      }
      body.theme-light .registration-step-index {
        background: rgba(37, 131, 255, 0.09);
        color: rgba(17, 76, 149, 0.9);
      }
      body.theme-dark .registration-step-index {
        background: rgba(96, 165, 250, 0.12);
        color: rgba(191, 219, 254, 0.92);
      }
      .registration-step-title {
        margin: 0;
        font-size: 1rem;
        font-weight: 700;
        letter-spacing: -0.02em;
      }
      .registration-step-copy {
        margin: 0.28rem 0 0;
        font-size: 0.82rem;
        line-height: 1.5;
      }
      body.theme-light .registration-step-copy {
        color: rgba(51, 65, 85, 0.78);
      }
      body.theme-dark .registration-step-copy {
        color: rgba(226, 232, 240, 0.72);
      }
      .track-grid,
      .scope-grid {
        display: grid;
        gap: 0.78rem;
      }
      .track-grid {
        grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
      }
      .scope-grid--cohorts {
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      }
      .scope-grid--campuses {
        grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
      }
      .track-option,
      .scope-option,
      .registration-empty {
        position: relative;
        border-radius: 20px;
        border: 1px solid rgba(148, 163, 184, 0.22);
        transition: transform 150ms ease, border-color 150ms ease, box-shadow 150ms ease, background 150ms ease, opacity 150ms ease;
      }
      body.theme-light .track-option,
      body.theme-light .scope-option,
      body.theme-light .registration-empty {
        background: rgba(255, 255, 255, 0.58);
        box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.76);
      }
      body.theme-dark .track-option,
      body.theme-dark .scope-option,
      body.theme-dark .registration-empty {
        background: rgba(255, 255, 255, 0.05);
        box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.05);
      }
      .track-option {
        padding: 0.85rem 0.95rem;
        text-align: center;
        cursor: pointer;
        font-weight: 700;
      }
      .track-option input {
        position: absolute;
        opacity: 0;
        pointer-events: none;
      }
      .track-option:hover,
      .scope-option:hover {
        transform: translateY(-1px);
      }
      .track-option.is-selected,
      .scope-option.is-selected {
        border-color: rgba(37, 131, 255, 0.58);
      }
      body.theme-light .track-option.is-selected,
      body.theme-light .scope-option.is-selected {
        background: linear-gradient(135deg, rgba(37, 131, 255, 0.14), rgba(78, 205, 196, 0.12));
        box-shadow: 0 18px 34px rgba(37, 131, 255, 0.12);
      }
      body.theme-dark .track-option.is-selected,
      body.theme-dark .scope-option.is-selected {
        background: linear-gradient(135deg, rgba(37, 131, 255, 0.18), rgba(18, 182, 201, 0.14));
        box-shadow: 0 18px 34px rgba(6, 20, 42, 0.26);
      }
      .track-option.is-disabled,
      .scope-option.is-disabled {
        cursor: not-allowed;
        opacity: 0.58;
      }
      .scope-option {
        width: 100%;
        padding: 0.95rem 1rem;
        text-align: left;
        cursor: pointer;
      }
      .scope-option[disabled] {
        cursor: not-allowed;
      }
      .scope-option-head,
      .scope-option-foot {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 0.7rem;
      }
      .scope-option-head {
        margin-bottom: 0.55rem;
      }
      .scope-option-title {
        font-size: 0.98rem;
        font-weight: 700;
        line-height: 1.25;
        letter-spacing: -0.02em;
      }
      .scope-option-subtitle {
        font-size: 0.78rem;
        line-height: 1.42;
        margin-top: 0.24rem;
      }
      body.theme-light .scope-option-subtitle,
      body.theme-light .registration-empty-copy,
      body.theme-light .registration-hint,
      body.theme-light .program-static-copy,
      body.theme-light .registration-summary-copy {
        color: rgba(51, 65, 85, 0.78);
      }
      body.theme-dark .scope-option-subtitle,
      body.theme-dark .registration-empty-copy,
      body.theme-dark .registration-hint,
      body.theme-dark .program-static-copy,
      body.theme-dark .registration-summary-copy {
        color: rgba(226, 232, 240, 0.74);
      }
      .scope-option-meta {
        display: flex;
        flex-wrap: wrap;
        gap: 0.42rem;
      }
      .scope-pill,
      .scope-status {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        min-height: 1.65rem;
        padding: 0.2rem 0.62rem;
        border-radius: 999px;
        font-size: 0.72rem;
        font-weight: 700;
        letter-spacing: -0.01em;
      }
      .scope-pill {
        border: 1px solid rgba(148, 163, 184, 0.22);
      }
      body.theme-light .scope-pill {
        background: rgba(255, 255, 255, 0.72);
        color: rgba(30, 41, 59, 0.76);
      }
      body.theme-dark .scope-pill {
        background: rgba(255, 255, 255, 0.08);
        color: rgba(226, 232, 240, 0.84);
      }
      .scope-status--ready {
        color: var(--success);
        background: rgba(17, 138, 86, 0.12);
      }
      .scope-status--blocked {
        color: var(--danger);
        background: rgba(207, 60, 68, 0.12);
      }
      .scope-status--needs_setup {
        color: var(--warning);
        background: rgba(184, 107, 0, 0.14);
      }
      body.theme-dark .scope-status--ready {
        color: #86efac;
        background: rgba(17, 138, 86, 0.16);
      }
      body.theme-dark .scope-status--blocked {
        color: #fda4af;
        background: rgba(207, 60, 68, 0.18);
      }
      body.theme-dark .scope-status--needs_setup {
        color: #fdba74;
        background: rgba(184, 107, 0, 0.18);
      }
      .registration-empty {
        padding: 0.95rem 1rem;
      }
      .registration-empty-title {
        font-size: 0.94rem;
        font-weight: 700;
        line-height: 1.25;
      }
      .registration-empty-copy {
        margin-top: 0.28rem;
        font-size: 0.79rem;
        line-height: 1.45;
      }
      .program-static {
        padding: 0.9rem 1rem;
        border-radius: 20px;
        border: 1px solid rgba(148, 163, 184, 0.2);
      }
      body.theme-light .program-static {
        background: rgba(255, 255, 255, 0.58);
      }
      body.theme-dark .program-static {
        background: rgba(255, 255, 255, 0.05);
      }
      .program-static-title {
        font-size: 0.96rem;
        font-weight: 700;
      }
      .program-static-copy {
        margin-top: 0.24rem;
        font-size: 0.79rem;
      }
      .pathway-select {
        min-height: 48px;
        border-radius: 18px;
        border: 1px solid rgba(148, 163, 184, 0.22);
        font-weight: 650;
      }
      body.theme-light .pathway-select {
        background: rgba(255, 255, 255, 0.76);
        color: #0f172a;
      }
      body.theme-dark .pathway-select {
        background: rgba(255, 255, 255, 0.08);
        color: #f8fafc;
      }
      body.page-register-course .pathway-select option {
        color: #0f172a;
        background: #ffffff;
      }
      body.theme-dark.page-register-course .pathway-select option {
        color: #f8fafc;
        background: #0f2238;
      }
      .registration-hint {
        margin: 0.65rem 0 0;
        font-size: 0.79rem;
        line-height: 1.45;
      }
      .registration-hint.is-error {
        color: var(--danger) !important;
      }
      .registration-summary {
        margin-top: 1rem;
        padding: 1rem 1.05rem 1.05rem;
        border-radius: 24px;
        border: 1px solid rgba(148, 163, 184, 0.16);
      }
      body.theme-light .registration-summary {
        background: linear-gradient(135deg, rgba(255, 255, 255, 0.76), rgba(241, 248, 255, 0.8));
      }
      body.theme-dark .registration-summary {
        background: linear-gradient(135deg, rgba(13, 28, 46, 0.84), rgba(10, 24, 39, 0.8));
      }
      .registration-summary-head {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 0.7rem;
      }
      .registration-summary-label {
        font-size: 0.78rem;
        font-weight: 700;
        letter-spacing: 0.01em;
        text-transform: uppercase;
      }
      body.theme-light .registration-summary-label {
        color: rgba(51, 65, 85, 0.68);
      }
      body.theme-dark .registration-summary-label {
        color: rgba(226, 232, 240, 0.62);
      }
      .registration-summary-title {
        margin: 0.75rem 0 0;
        font-size: 1.02rem;
        line-height: 1.38;
        font-weight: 700;
        letter-spacing: -0.02em;
      }
      .registration-summary-copy {
        margin: 0.35rem 0 0;
        font-size: 0.82rem;
        line-height: 1.5;
      }
      .summary-chip-row {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
        margin-top: 0.85rem;
      }
      .summary-chip {
        display: inline-flex;
        align-items: center;
        gap: 0.38rem;
        min-height: 1.75rem;
        padding: 0.24rem 0.68rem;
        border-radius: 999px;
        font-size: 0.74rem;
        font-weight: 650;
        border: 1px solid rgba(148, 163, 184, 0.2);
      }
      body.theme-light .summary-chip {
        background: rgba(255, 255, 255, 0.72);
        color: rgba(30, 41, 59, 0.84);
      }
      body.theme-dark .summary-chip {
        background: rgba(255, 255, 255, 0.08);
        color: rgba(226, 232, 240, 0.88);
      }
      .auth-action-stack {
        margin-top: 1.15rem;
      }
      body.page-register-course .register-action-btn {
        height: 48px;
        border-radius: 16px;
        font-weight: 700;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 0.78rem 1rem;
      }
      body.page-register-course .btn-primary.register-action-btn {
        background: linear-gradient(180deg, #2d8cff, #1973e8);
        border: 1px solid rgba(37, 131, 255, 0.52);
        box-shadow: 0 18px 34px rgba(37, 131, 255, 0.18);
      }
      body.page-register-course .btn-primary.register-action-btn:disabled {
        opacity: 0.58;
        box-shadow: none;
      }
      body.page-register-course .btn-outline-primary.register-action-btn {
        border-radius: 16px;
      }
      .theme-toggle-floating {
        position: fixed;
        top: 16px;
        right: 16px;
        z-index: 10;
      }
      .theme-toggle-min {
        width: 42px;
        height: 42px;
        border-radius: 999px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        border: 1px solid rgba(255, 255, 255, 0.22);
        background: rgba(255, 255, 255, 0.08);
        color: inherit;
        backdrop-filter: blur(12px);
      }
      body.theme-light .theme-toggle-min {
        border-color: rgba(15, 23, 42, 0.12);
        background: rgba(255, 255, 255, 0.74);
      }
      .btn {
        transition: transform 140ms ease, box-shadow 140ms ease;
      }
      .btn:hover:not(:disabled) {
        transform: translateY(-1px);
      }
      .btn:active:not(:disabled) {
        transform: translateY(0);
      }
      .app-footer {
        font-size: 0.8rem;
      }
      body.theme-dark .app-footer {
        color: rgba(255, 255, 255, 0.62);
      }
      body.theme-light .app-footer {
        color: rgba(15, 23, 42, 0.6);
      }
      @media (max-width: 767px) {
        body.page-register-course .auth-card .card-body {
          padding: 1.35rem 1.05rem !important;
        }
        .registration-step-header {
          gap: 0.7rem;
        }
        .scope-grid--cohorts,
        .scope-grid--campuses {
          grid-template-columns: 1fr;
        }
      }
    </style>
    <link rel="stylesheet" href="/css/tokens.css" />
    <link rel="stylesheet" href="/css/components.css" />
    <link rel="stylesheet" href="/css/pages/auth.css" />
    <link rel="stylesheet" href="/css/auth-actions.css" />
    <link rel="stylesheet" href="/css/design-system.css" />
    <link rel="stylesheet" href="/css/studerria-ui.css" />
    <link rel="stylesheet" href="/css/dynamic-bg.css" />
  </head>
  <body class="studerria-theme theme-light page-register-course">
    <%- include('partials/theme-bootstrap-body') %>
    <%- include('partials/dynamic-bg') %>
    <div class="theme-toggle-floating">
      <button
        class="theme-toggle-min"
        type="button"
        id="themeToggle"
        aria-label="<%= registerCourseText.themeToggleAria %>"
        data-light-label="☀️"
        data-dark-label="🌙"
      >
        🌙
      </button>
    </div>
    <div class="container">
      <div class="row justify-content-center align-items-center" style="min-height: 100vh;">
        <div class="col-11 col-md-10 col-lg-8 col-xl-7">
          <div class="card auth-card glass">
            <div class="card-body">
              <div class="registration-intro glass">
                <span class="registration-eyebrow"><%= registerCourseText.introLabel %></span>
                <div class="registration-intro-copy">
                  <h1><%= registerCourseText.introTitle %></h1>
                  <p><%= registerCourseText.introSubtitle %></p>
                </div>
              </div>
              <% if (error) { %>
              <div class="alert alert-danger" role="alert"><%= error %></div>
              <% } %>
              <form method="POST" action="/register/course" id="registerCourseForm">
                <section class="registration-step">
                  <div class="registration-step-header">
                    <span class="registration-step-index">1</span>
                    <div>
                      <h2 class="registration-step-title"><%= registerCourseText.stepTrack %></h2>
                      <p class="registration-step-copy"><%= registerCourseText.stepTrackCopy %></p>
                    </div>
                  </div>
                  <div class="track-grid" id="trackGrid">
                    <% (scopeMatrix.tracks || []).forEach((track) => { %>
                      <label class="track-option <%= track.enabled ? '' : 'is-disabled' %>" data-track-card="<%= track.key %>">
                        <input
                          type="radio"
                          name="study_track"
                          value="<%= track.key %>"
                          <%= selectedTrackValue === track.key ? 'checked' : '' %>
                          <%= track.enabled ? '' : 'disabled' %>
                          required
                        />
                        <span><%= formatTrackLabel(track.key, track.label) %></span>
                      </label>
                    <% }) %>
                  </div>
                </section>

                <section class="registration-step">
                  <div class="registration-step-header">
                    <span class="registration-step-index">2</span>
                    <div>
                      <h2 class="registration-step-title"><%= registerCourseText.stepProgram %></h2>
                      <p class="registration-step-copy"><%= registerCourseText.stepProgramCopy %></p>
                    </div>
                  </div>
                  <div id="programField">
                    <label class="visually-hidden" for="studyProgramSelect"><%= registerCourseText.stepProgram %></label>
                    <select id="studyProgramSelect" name="program_id" class="form-select pathway-select"></select>
                  </div>
                  <div id="programStaticField" style="display:none;">
                    <div class="program-static">
                      <div class="program-static-title" id="programStaticLabel"></div>
                      <div class="program-static-copy" id="programStaticCopy"><%= registerCourseText.stepProgramCopy %></div>
                    </div>
                  </div>
                  <p class="registration-hint" id="programHint"></p>
                </section>

                <section class="registration-step" id="cohortStep">
                  <div class="registration-step-header">
                    <span class="registration-step-index">3</span>
                    <div>
                      <h2 class="registration-step-title"><%= registerCourseText.stepCohort %></h2>
                      <p class="registration-step-copy"><%= registerCourseText.stepCohortCopy %></p>
                    </div>
                  </div>
                  <input type="hidden" name="cohort_id" id="selectedCohortInput" value="" />
                  <div class="scope-grid scope-grid--cohorts" id="cohortOptions"></div>
                  <p class="registration-hint" id="cohortHint"></p>
                </section>

                <section class="registration-step" id="campusStep">
                  <div class="registration-step-header">
                    <span class="registration-step-index">4</span>
                    <div>
                      <h2 class="registration-step-title"><%= registerCourseText.stepCampus %></h2>
                      <p class="registration-step-copy" id="campusStepCopy"><%= registerCourseText.stepCampusCopyStudent %></p>
                    </div>
                  </div>
                  <input type="hidden" name="group_id" id="selectedGroupInput" value="" />
                  <input type="hidden" name="campus" id="selectedCampusInput" value="" />
                  <div class="scope-grid scope-grid--campuses" id="campusOptions"></div>
                  <p class="registration-hint" id="campusHint"></p>
                </section>

                <div class="registration-summary glass" id="academicContextSummaryCard">
                  <div class="registration-summary-head">
                    <span class="registration-summary-label"><%= registerCourseText.summaryLabel %></span>
                    <span class="scope-status scope-status--needs_setup" id="summaryStatusBadge"><%= registerCourseText.summaryPending %></span>
                  </div>
                  <div class="registration-summary-title" id="academicContextSummary">
                    <%= selectedTrackValue === 'teacher' ? registerCourseText.summaryEmptyTeacher : registerCourseText.summaryEmptyStudent %>
                  </div>
                  <p class="registration-summary-copy" id="academicContextSummaryCopy">
                    <%= selectedTrackValue === 'teacher' ? registerCourseText.continueLockedTeacher : registerCourseText.continueLockedStudent %>
                  </p>
                  <div class="summary-chip-row" id="summaryChips"></div>
                </div>

                <div class="auth-action-stack auth-action-stack--grid">
                  <button type="submit" class="btn btn-primary register-action-btn auth-action-btn" id="registerContinueButton" disabled>
                    <%= t('registerCourse.continue') %>
                  </button>
                  <a href="/register" class="btn btn-outline-primary register-action-btn auth-action-btn"><%= t('registerCourse.backToRegister') %></a>
                </div>
              </form>
            </div>
          </div>
          <%- include('partials/app-footer') %>
        </div>
      </div>
    </div>
    <%- include('partials/changelog-modal') %>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
    <script>
      const currentLang = <%- JSON.stringify(lang) %>;
      const registerCourseText = <%- JSON.stringify(registerCourseText) %>;
      const scopeMatrix = <%- JSON.stringify(scopeMatrix) %>;
      const trackInputs = Array.from(document.querySelectorAll('input[name="study_track"]'));
      const programSelect = document.getElementById('studyProgramSelect');
      const programField = document.getElementById('programField');
      const programStaticField = document.getElementById('programStaticField');
      const programStaticLabel = document.getElementById('programStaticLabel');
      const programHint = document.getElementById('programHint');
      const cohortStep = document.getElementById('cohortStep');
      const cohortOptions = document.getElementById('cohortOptions');
      const cohortHint = document.getElementById('cohortHint');
      const cohortInput = document.getElementById('selectedCohortInput');
      const campusStep = document.getElementById('campusStep');
      const campusStepCopy = document.getElementById('campusStepCopy');
      const campusOptions = document.getElementById('campusOptions');
      const campusHint = document.getElementById('campusHint');
      const groupInput = document.getElementById('selectedGroupInput');
      const campusInput = document.getElementById('selectedCampusInput');
      const academicContextSummaryEl = document.getElementById('academicContextSummary');
      const academicContextSummaryCopyEl = document.getElementById('academicContextSummaryCopy');
      const summaryStatusBadge = document.getElementById('summaryStatusBadge');
      const summaryChips = document.getElementById('summaryChips');
      const continueButton = document.getElementById('registerContinueButton');
      const registerCourseForm = document.getElementById('registerCourseForm');

      const state = {
        trackKey: '',
        programId: null,
        cohortId: null,
        campusKey: '',
        groupId: null,
        studyContextId: null,
        courseId: null,
        cohortConfirmed: false,
        campusConfirmed: false,
      };

      function escapeHtml(rawValue) {
        return String(rawValue || '')
          .replace(/&/g, '&amp;')
          .replace(/</g, '&lt;')
          .replace(/>/g, '&gt;')
          .replace(/"/g, '&quot;')
          .replace(/'/g, '&#39;');
      }

      function normalizeTrackKey(rawValue) {
        const normalized = String(rawValue || '').trim().toLowerCase();
        if (normalized === 'master') return 'master';
        if (normalized === 'teacher') return 'teacher';
        return 'bachelor';
      }

      function normalizeCampusKey(rawValue) {
        return String(rawValue || '').trim().toLowerCase() === 'munich' ? 'munich' : 'kyiv';
      }

      function isTeacherTrack(trackKey) {
        return normalizeTrackKey(trackKey) === 'teacher';
      }

      function formatTrackLabelClient(trackKey) {
        const normalized = normalizeTrackKey(trackKey);
        if (normalized === 'master') return currentLang === 'en' ? "Master's" : 'Магістратура';
        if (normalized === 'teacher') return currentLang === 'en' ? 'Teacher' : 'Викладач';
        return currentLang === 'en' ? 'Bachelor' : 'Бакалаврат';
      }

      function formatCampusLabelClient(campusKey) {
        return normalizeCampusKey(campusKey) === 'munich'
          ? registerCourseText.campusMunich
          : registerCourseText.campusKyiv;
      }

      function getTrackOptions() {
        return Array.isArray(scopeMatrix.tracks) ? scopeMatrix.tracks : [];
      }

      function getProgramsForTrack(trackKey) {
        return (Array.isArray(scopeMatrix.programs) ? scopeMatrix.programs : [])
          .filter((program) => normalizeTrackKey(program && program.track_key) === normalizeTrackKey(trackKey));
      }

      function getProgramById(programId) {
        return (Array.isArray(scopeMatrix.programs) ? scopeMatrix.programs : [])
          .find((program) => Number(program.id || 0) === Number(programId || 0)) || null;
      }

      function getCohortsForProgram(programId) {
        return (scopeMatrix && scopeMatrix.cohortsByProgram && scopeMatrix.cohortsByProgram[String(programId)]) || [];
      }

      function getCampusesForCohort(cohortId) {
        return (scopeMatrix && scopeMatrix.campusesByCohort && scopeMatrix.campusesByCohort[String(cohortId)]) || [];
      }

      function getTeacherCampusesForProgram(programId) {
        return (scopeMatrix && scopeMatrix.teacherCampusesByProgram && scopeMatrix.teacherCampusesByProgram[String(programId)]) || [];
      }

      function resolveTrackKey(preferredTrack) {
        const normalizedPreferred = normalizeTrackKey(preferredTrack);
        const trackRows = getTrackOptions();
        if (trackRows.some((track) => track && track.enabled && normalizeTrackKey(track.key) === normalizedPreferred)) {
          return normalizedPreferred;
        }
        return normalizeTrackKey((trackRows.find((track) => track && track.enabled) || {}).key || 'bachelor');
      }

      function resolveProgramId(trackKey, preferredProgramId) {
        const programs = getProgramsForTrack(trackKey);
        if (programs.some((program) => Number(program.id || 0) === Number(preferredProgramId || 0))) {
          return Number(preferredProgramId || 0);
        }
        return programs[0] ? Number(programs[0].id || 0) : null;
      }

      function formatProgramLabel(program) {
        if (!program) return '';
        return program.code ? `${program.name} (${program.code})` : String(program.name || '').trim();
      }

      function getCampusReadyLabel(count, total) {
        if (Number(count || 0) <= 0) {
          return registerCourseText.noReadyCampuses;
        }
        if (Number(count || 0) === 1) {
          return `${registerCourseText.availableCampusesOne} / ${total} ${registerCourseText.totalCampusesLabel}`;
        }
        return `${count} ${registerCourseText.availableCampusesMany} / ${total} ${registerCourseText.totalCampusesLabel}`;
      }

      function setHint(target, message, isError = false) {
        if (!(target instanceof HTMLElement)) return;
        target.textContent = message || '';
        target.classList.toggle('is-error', Boolean(isError));
      }

      function clearSelectionBelowProgram() {
        state.cohortId = null;
        state.campusKey = '';
        state.groupId = null;
        state.studyContextId = null;
        state.courseId = null;
        state.cohortConfirmed = false;
        state.campusConfirmed = false;
      }

      function clearCampusSelection() {
        state.campusKey = '';
        state.groupId = null;
        state.studyContextId = null;
        state.courseId = null;
        state.campusConfirmed = false;
      }

      function isSelectionComplete() {
        if (isTeacherTrack(state.trackKey)) {
          return Boolean(state.programId && state.campusConfirmed && state.groupId);
        }
        return Boolean(state.programId && state.cohortConfirmed && state.campusConfirmed && state.groupId);
      }

      function syncTrackCards() {
        document.querySelectorAll('[data-track-card]').forEach((card) => {
          const trackKey = normalizeTrackKey(card.dataset.trackCard);
          const selected = trackKey === normalizeTrackKey(state.trackKey);
          card.classList.toggle('is-selected', selected);
          const input = card.querySelector('input[name="study_track"]');
          if (input) {
            input.checked = selected;
          }
        });
      }

      function renderProgramField() {
        const programs = getProgramsForTrack(state.trackKey);
        if (!programSelect || !programField || !programStaticField) return;
        const resolvedProgramId = resolveProgramId(state.trackKey, state.programId);
        if (Number(resolvedProgramId || 0) !== Number(state.programId || 0)) {
          state.programId = resolvedProgramId;
          clearSelectionBelowProgram();
        }
        if (!programs.length) {
          programField.style.display = '';
          programStaticField.style.display = 'none';
          programSelect.innerHTML = '';
          setHint(programHint, registerCourseText.noPrograms, true);
          return;
        }
        if (programs.length === 1) {
          const onlyProgram = programs[0];
          programSelect.innerHTML = '';
          const option = document.createElement('option');
          option.value = String(onlyProgram.id || '');
          option.textContent = formatProgramLabel(onlyProgram);
          option.selected = true;
          programSelect.appendChild(option);
          programField.style.display = 'none';
          programStaticField.style.display = '';
          if (programStaticLabel) {
            programStaticLabel.textContent = formatProgramLabel(onlyProgram);
          }
          state.programId = Number(onlyProgram.id || 0);
          setHint(programHint, '');
          return;
        }
        programField.style.display = '';
        programStaticField.style.display = 'none';
        programSelect.innerHTML = '';
        programs.forEach((program) => {
          const option = document.createElement('option');
          option.value = String(program.id || '');
          option.textContent = formatProgramLabel(program);
          if (Number(program.id || 0) === Number(state.programId || 0)) {
            option.selected = true;
          }
          programSelect.appendChild(option);
        });
        setHint(programHint, '');
      }

      function createEmptyCard(title, copy) {
        const empty = document.createElement('div');
        empty.className = 'registration-empty';
        empty.innerHTML = `
          <div class="registration-empty-title">${escapeHtml(title)}</div>
          ${copy ? `<div class="registration-empty-copy">${escapeHtml(copy)}</div>` : ''}
        `;
        return empty;
      }

      function renderCohorts() {
        if (!(cohortOptions instanceof HTMLElement) || !(cohortStep instanceof HTMLElement)) return;
        const teacherMode = isTeacherTrack(state.trackKey);
        cohortStep.style.display = teacherMode ? 'none' : '';
        cohortOptions.innerHTML = '';
        if (teacherMode) {
          setHint(cohortHint, '');
          return;
        }
        const cohorts = getCohortsForProgram(state.programId);
        if (!state.programId) {
          cohortOptions.appendChild(createEmptyCard(registerCourseText.chooseProgram, registerCourseText.chooseProgramFirst));
          setHint(cohortHint, registerCourseText.chooseProgramFirst);
          clearSelectionBelowProgram();
          return;
        }
        if (!cohorts.length) {
          cohortOptions.appendChild(createEmptyCard(registerCourseText.noCohorts, registerCourseText.noReadyCohorts));
          setHint(cohortHint, registerCourseText.noReadyCohorts, true);
          clearSelectionBelowProgram();
          return;
        }
        const hasReadyCohort = cohorts.some((cohort) => cohort && cohort.is_ready);
        if (!hasReadyCohort) {
          setHint(cohortHint, registerCourseText.noReadyCohorts, true);
        } else if (!state.cohortConfirmed) {
          setHint(cohortHint, registerCourseText.chooseCohortFirst);
        } else {
          const activeCohort = cohorts.find((cohort) => Number(cohort.id || 0) === Number(state.cohortId || 0)) || null;
          setHint(cohortHint, activeCohort ? activeCohort.reason_copy : registerCourseText.chooseCohortFirst);
        }
        cohorts.forEach((cohort) => {
          const button = document.createElement('button');
          const isSelectable = Boolean(cohort && cohort.is_ready);
          const isSelected = state.cohortConfirmed && Number(cohort.id || 0) === Number(state.cohortId || 0);
          button.type = 'button';
          button.className = `scope-option ${isSelected ? 'is-selected' : ''} ${isSelectable ? '' : 'is-disabled'}`;
          button.disabled = !isSelectable;
          const statusClass = String(cohort.status_key || 'needs_setup').replace('-', '_');
          const cohortLabel = String(cohort.label || '').trim() || `${registerCourseText.cohortLabel} ${Number(cohort.admission_year || 0) || ''}`;
          const availableLabel = getCampusReadyLabel(cohort.available_campuses_count, cohort.total_campuses_count);
          button.innerHTML = `
            <div class="scope-option-head">
              <div class="scope-option-title">${escapeHtml(cohortLabel)}</div>
              <span class="scope-status scope-status--${escapeHtml(statusClass)}">${escapeHtml(cohort.status_label || registerCourseText.needsSetup)}</span>
            </div>
            <div class="scope-option-subtitle">${escapeHtml(`${registerCourseText.admissionYearLabel} ${Number(cohort.admission_year || 0) || ''}`)}</div>
            <div class="scope-option-subtitle">${escapeHtml(cohort.reason_copy || '')}</div>
            <div class="scope-option-foot">
              <div class="scope-option-meta">
                <span class="scope-pill">${escapeHtml(availableLabel)}</span>
              </div>
            </div>
          `;
          if (isSelectable) {
            button.addEventListener('click', () => {
              state.cohortId = Number(cohort.id || 0) || null;
              state.cohortConfirmed = true;
              clearCampusSelection();
              renderAll();
            });
          }
          cohortOptions.appendChild(button);
        });
      }

      function renderCampuses() {
        if (!(campusOptions instanceof HTMLElement)) return;
        const teacherMode = isTeacherTrack(state.trackKey);
        campusOptions.innerHTML = '';
        if (campusStepCopy) {
          campusStepCopy.textContent = teacherMode
            ? registerCourseText.stepCampusCopyTeacher
            : registerCourseText.stepCampusCopyStudent;
        }
        if (teacherMode) {
          if (!state.programId) {
            campusOptions.appendChild(createEmptyCard(registerCourseText.chooseProgram, registerCourseText.chooseProgramFirst));
            setHint(campusHint, registerCourseText.chooseProgramFirst);
            clearCampusSelection();
            return;
          }
        } else if (!state.cohortConfirmed || !state.cohortId) {
          campusOptions.appendChild(createEmptyCard(registerCourseText.cohortLabel, registerCourseText.chooseCohortFirst));
          setHint(campusHint, registerCourseText.chooseCohortFirst);
          clearCampusSelection();
          return;
        }

        const campusChoices = teacherMode
          ? getTeacherCampusesForProgram(state.programId)
          : getCampusesForCohort(state.cohortId);

        if (!campusChoices.length) {
          campusOptions.appendChild(createEmptyCard(registerCourseText.noCampuses, registerCourseText.noReadyCampuses));
          setHint(campusHint, registerCourseText.noReadyCampuses, true);
          clearCampusSelection();
          return;
        }

        const hasSelectableCampus = campusChoices.some((choice) => choice && choice.is_selectable);
        if (!hasSelectableCampus) {
          setHint(campusHint, registerCourseText.noReadyCampuses, true);
        } else if (!state.campusConfirmed) {
          setHint(campusHint, registerCourseText.chooseCampus);
        } else {
          const activeChoice = campusChoices.find((choice) => normalizeCampusKey(choice.campus_key) === normalizeCampusKey(state.campusKey)) || null;
          setHint(campusHint, activeChoice ? activeChoice.reason_copy : registerCourseText.chooseCampus);
        }

        campusChoices.forEach((choice) => {
          const button = document.createElement('button');
          const isSelectable = Boolean(choice && choice.is_selectable);
          const isSelected = state.campusConfirmed
            && normalizeCampusKey(choice.campus_key) === normalizeCampusKey(state.campusKey)
            && Number(choice.group_id || 0) === Number(state.groupId || 0);
          const statusClass = isSelectable ? 'ready' : (choice.reason_tone === 'blocked' ? 'blocked' : 'needs_setup');
          button.type = 'button';
          button.className = `scope-option ${isSelected ? 'is-selected' : ''} ${isSelectable ? '' : 'is-disabled'}`;
          button.disabled = !isSelectable;
          button.innerHTML = `
            <div class="scope-option-head">
              <div class="scope-option-title">${escapeHtml(choice.campus_label || formatCampusLabelClient(choice.campus_key))}</div>
              <span class="scope-status scope-status--${escapeHtml(statusClass)}">${escapeHtml(isSelectable ? registerCourseText.ready : (choice.reason_short || registerCourseText.needsSetup))}</span>
            </div>
            <div class="scope-option-subtitle">${escapeHtml(choice.reason_copy || '')}</div>
            <div class="scope-option-foot">
              <div class="scope-option-meta">
                ${choice.group_label ? `<span class="scope-pill">${escapeHtml(choice.group_label)}</span>` : ''}
              </div>
            </div>
          `;
          if (isSelectable) {
            button.addEventListener('click', () => {
              state.campusKey = normalizeCampusKey(choice.campus_key);
              state.groupId = Number(choice.group_id || 0) || null;
              state.studyContextId = Number(choice.study_context_id || choice.group_id || 0) || null;
              state.courseId = Number(choice.course_id || 0) || null;
              if (!teacherMode) {
                state.cohortId = Number(choice.cohort_id || state.cohortId || 0) || state.cohortId;
                state.cohortConfirmed = true;
              }
              state.campusConfirmed = true;
              renderAll();
            });
          }
          campusOptions.appendChild(button);
        });
      }

      function syncFormFields() {
        if (cohortInput) {
          cohortInput.value = !isTeacherTrack(state.trackKey) && state.cohortConfirmed
            ? String(state.cohortId || '')
            : '';
        }
        if (groupInput) {
          groupInput.value = state.campusConfirmed ? String(state.groupId || '') : '';
        }
        if (campusInput) {
          campusInput.value = state.campusConfirmed ? String(state.campusKey || '') : '';
        }
        if (programSelect && programField.style.display !== 'none') {
          programSelect.value = state.programId ? String(state.programId) : '';
        }
        if (continueButton) {
          continueButton.disabled = !isSelectionComplete();
        }
      }

      function renderSummary() {
        if (!(academicContextSummaryEl instanceof HTMLElement) || !(summaryStatusBadge instanceof HTMLElement)) return;
        const program = getProgramById(state.programId);
        const teacherMode = isTeacherTrack(state.trackKey);
        const defaultTitle = teacherMode
          ? registerCourseText.summaryEmptyTeacher
          : registerCourseText.summaryEmptyStudent;
        const chips = [];
        chips.push(`${registerCourseText.scopeRoute}: ${formatTrackLabelClient(state.trackKey)}`);
        if (program) {
          chips.push(`${registerCourseText.scopeProgram}: ${formatProgramLabel(program)}`);
        }
        if (!teacherMode && state.cohortConfirmed && state.cohortId) {
          const cohort = getCohortsForProgram(state.programId)
            .find((item) => Number(item.id || 0) === Number(state.cohortId || 0)) || null;
          if (cohort) {
            chips.push(`${registerCourseText.scopeCohort}: ${cohort.label || `${registerCourseText.cohortLabel} ${cohort.admission_year}`}`);
          }
        }
        if (state.campusConfirmed && state.campusKey) {
          chips.push(`${registerCourseText.campusLabel}: ${formatCampusLabelClient(state.campusKey)}`);
        }

        let groupLabel = '';
        if (state.campusConfirmed && state.groupId) {
          const campusChoices = teacherMode
            ? getTeacherCampusesForProgram(state.programId)
            : getCampusesForCohort(state.cohortId);
          const activeChoice = campusChoices.find((choice) => Number(choice.group_id || 0) === Number(state.groupId || 0)) || null;
          if (activeChoice && activeChoice.group_label) {
            groupLabel = activeChoice.group_label;
            chips.push(`${registerCourseText.scopeGroup}: ${activeChoice.group_label}`);
          }
        }

        if (isSelectionComplete()) {
          const summaryParts = [
            formatTrackLabelClient(state.trackKey),
            program ? formatProgramLabel(program) : '',
            !teacherMode && state.cohortConfirmed && state.cohortId
              ? (() => {
                const cohort = getCohortsForProgram(state.programId)
                  .find((item) => Number(item.id || 0) === Number(state.cohortId || 0)) || null;
                return cohort ? (cohort.label || `${registerCourseText.cohortLabel} ${cohort.admission_year}`) : '';
              })()
              : '',
            state.campusConfirmed ? formatCampusLabelClient(state.campusKey) : '',
            groupLabel,
          ].filter(Boolean);
          academicContextSummaryEl.textContent = summaryParts.join(' / ');
          academicContextSummaryCopyEl.textContent = teacherMode
            ? registerCourseText.ready
            : registerCourseText.ready;
          summaryStatusBadge.textContent = registerCourseText.summaryReady;
          summaryStatusBadge.className = 'scope-status scope-status--ready';
        } else {
          academicContextSummaryEl.textContent = defaultTitle;
          academicContextSummaryCopyEl.textContent = teacherMode
            ? registerCourseText.continueLockedTeacher
            : registerCourseText.continueLockedStudent;
          summaryStatusBadge.textContent = registerCourseText.summaryPending;
          summaryStatusBadge.className = 'scope-status scope-status--needs_setup';
        }

        if (summaryChips) {
          summaryChips.innerHTML = '';
          chips.forEach((label) => {
            const chip = document.createElement('span');
            chip.className = 'summary-chip';
            chip.textContent = label;
            summaryChips.appendChild(chip);
          });
        }
      }

      function renderAll() {
        syncTrackCards();
        renderProgramField();
        renderCohorts();
        renderCampuses();
        syncFormFields();
        renderSummary();
      }

      function hydrateInitialState() {
        const initial = scopeMatrix && scopeMatrix.initialSelection ? scopeMatrix.initialSelection : {};
        state.trackKey = resolveTrackKey(initial.trackKey || selectedTrackValue);
        state.programId = resolveProgramId(state.trackKey, initial.programId);
        if (initial && initial.campusConfirmed && Number(initial.groupId || 0) > 0) {
          state.cohortId = Number(initial.cohortId || 0) || null;
          state.campusKey = normalizeCampusKey(initial.campusKey);
          state.groupId = Number(initial.groupId || 0) || null;
          state.studyContextId = Number(initial.studyContextId || initial.groupId || 0) || null;
          state.courseId = Number(initial.courseId || 0) || null;
          state.campusConfirmed = true;
          state.cohortConfirmed = !isTeacherTrack(state.trackKey);
          if (isTeacherTrack(state.trackKey)) {
            state.cohortConfirmed = false;
          }
        }
      }

      trackInputs.forEach((input) => {
        input.addEventListener('change', () => {
          const nextTrackKey = resolveTrackKey(input.value);
          if (normalizeTrackKey(nextTrackKey) === normalizeTrackKey(state.trackKey)) {
            return;
          }
          state.trackKey = nextTrackKey;
          state.programId = resolveProgramId(state.trackKey, null);
          clearSelectionBelowProgram();
          renderAll();
        });
      });

      if (programSelect) {
        programSelect.addEventListener('change', () => {
          const nextProgramId = Number(programSelect.value || 0) || null;
          if (Number(nextProgramId || 0) === Number(state.programId || 0)) {
            return;
          }
          state.programId = nextProgramId;
          clearSelectionBelowProgram();
          renderAll();
        });
      }

      if (registerCourseForm) {
        registerCourseForm.addEventListener('submit', (event) => {
          if (!isSelectionComplete()) {
            event.preventDefault();
            setHint(
              campusHint,
              isTeacherTrack(state.trackKey) ? registerCourseText.continueLockedTeacher : registerCourseText.continueLockedStudent,
              true
            );
          }
        });
      }

      hydrateInitialState();
      renderAll();

      const themeToggle = document.getElementById('themeToggle');
      const savedTheme = localStorage.getItem('ui-theme') || 'theme-light';
      document.body.classList.remove('theme-light', 'theme-dark');
      document.body.classList.add(savedTheme);
      themeToggle.textContent = savedTheme === 'theme-dark' ? themeToggle.dataset.lightLabel : themeToggle.dataset.darkLabel;
      themeToggle.addEventListener('click', () => {
        const next = document.body.classList.contains('theme-dark') ? 'theme-light' : 'theme-dark';
        document.body.classList.remove('theme-light', 'theme-dark');
        document.body.classList.add(next);
        localStorage.setItem('ui-theme', next);
        themeToggle.textContent = next === 'theme-dark' ? themeToggle.dataset.lightLabel : themeToggle.dataset.darkLabel;
      });
    </script>
    <script src="/js/design-system.js" defer></script>
    <script src="/js/dynamic-bg.js" defer></script>
  </body>
</html>
