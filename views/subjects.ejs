<!doctype html>
<html lang="uk">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Предмети • Studerria</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/tokens.css" />
    <link rel="stylesheet" href="/css/components.css" />
    <link rel="stylesheet" href="/css/pages/subjects.css" />
    <link rel="stylesheet" href="/css/design-system.css" />
  </head>
  <body class="theme-dark page-subjects">
    <%- include('partials/topbar', { role, username, t, activePage: 'subjects', pageTitle: 'Предмети' }) %>

    <main class="container py-4">
      <% if (messages && messages.error) { %>
        <div class="alert alert-danger"><%= messages.error %></div>
      <% } %>
      <% if (messages && messages.success) { %>
        <div class="alert alert-success"><%= messages.success %></div>
      <% } %>

      <div class="glass glass-float glass-float--no-top-glare subject-picker-card p-3 mb-4">
        <div class="d-flex flex-wrap align-items-start justify-content-between gap-2 mb-3">
          <div>
            <h5 class="mb-1">Предмети</h5>
            <% if (selectedSubject) { %>
              <div class="text-muted small">
                <span class="course-badge"><%= selectedSubject.course_name %></span>
                <span class="ms-2"><%= selectedSubject.group_label %></span>
              </div>
            <% } %>
          </div>
          <span class="status-chip status-chip--info"><%= isTeacherMode ? 'Викладач' : 'Студент' %></span>
        </div>
        <div class="subject-chip-grid">
          <% if (!subjects || !subjects.length) { %>
            <span class="text-muted">Наразі немає доступних предметів.</span>
          <% } else { %>
            <% subjects.forEach(function(subject) { %>
              <a
                class="subject-nav-pill <%= selectedSubjectId === subject.subject_id ? 'is-active' : '' %>"
                href="/subjects?subject_id=<%= subject.subject_id %>"
              >
                <span class="subject-name"><%= subject.subject_name %></span>
                <span class="subject-meta"><%= subject.course_name %> • <%= subject.group_label %></span>
              </a>
            <% }); %>
          <% } %>
        </div>
      </div>

      <% if (selectedSubject) { %>
        <div class="living-separator mb-4" aria-hidden="true"></div>

        <% if (isTeacherMode) { %>
          <section class="glass materials-add-form mb-4">
            <div class="materials-toolbar mb-3">
              <h6 class="mb-0">Додати матеріал</h6>
              <span class="text-muted small">Лекції, файли та посилання для студентів</span>
            </div>
            <form method="POST" action="/subjects/materials" enctype="multipart/form-data">
              <input type="hidden" name="subject_id" value="<%= selectedSubject.subject_id %>" />
              <div class="row g-3">
                <div class="col-12 col-lg-5">
                  <label class="form-label">Назва</label>
                  <input class="form-control" name="title" maxlength="180" placeholder="Наприклад: Лекція 3 — міжнародні режими" required />
                </div>
                <div class="col-12 col-md-6 col-lg-3">
                  <label class="form-label">Тип</label>
                  <select class="form-select" name="material_type">
                    <option value="lecture">Лекція</option>
                    <option value="file">Файл</option>
                    <option value="link">Посилання</option>
                    <option value="mixed">Змішаний</option>
                  </select>
                </div>
                <div class="col-12 col-md-6 col-lg-4">
                  <label class="form-label">Для групи</label>
                  <select class="form-select" name="group_number">
                    <% (materialAudienceOptions || []).forEach(function(option) { %>
                      <option value="<%= option.value %>"><%= option.label %></option>
                    <% }); %>
                  </select>
                </div>
                <div class="col-12 col-lg-6">
                  <label class="form-label">Посилання (опц.)</label>
                  <input class="form-control" name="link_url" placeholder="https://example.com/lecture" />
                </div>
                <div class="col-12 col-lg-6">
                  <label class="form-label">Файл (опц.)</label>
                  <input class="form-control" type="file" name="attachment" />
                </div>
                <div class="col-12">
                  <label class="form-label">Опис</label>
                  <textarea class="form-control" name="description" rows="3" maxlength="4000" placeholder="Короткий план, питання до обговорення, інструкції..."></textarea>
                </div>
                <div class="col-12">
                  <button class="btn btn-primary" type="submit">Опублікувати матеріал</button>
                </div>
              </div>
            </form>
          </section>
        <% } %>

        <section class="materials-feed">
          <% const typeLabelMap = { lecture: 'Лекція', file: 'Файл', link: 'Посилання', mixed: 'Змішаний' }; %>
          <% if (!materials || !materials.length) { %>
            <div class="glass p-4 text-center text-muted">
              <% if (isTeacherMode) { %>
                Ще немає матеріалів для цього предмета.
              <% } else { %>
                Викладач ще не додав матеріали для цього предмета.
              <% } %>
            </div>
          <% } else { %>
            <% materials.forEach(function(item) { %>
              <article class="glass material-card">
                <div class="material-header">
                  <div>
                    <h6 class="material-title"><%= item.title %></h6>
                    <div class="material-meta">
                      <span><%= item.group_label %></span>
                      <span class="mx-1">•</span>
                      <span><%= item.created_by_name %></span>
                      <span class="mx-1">•</span>
                      <span><%= item.created_at_label %></span>
                    </div>
                  </div>
                  <span class="status-chip status-chip--neutral material-type-chip"><%= typeLabelMap[item.material_type] || 'Матеріал' %></span>
                </div>

                <% if (item.description) { %>
                  <div class="material-body"><%= item.description %></div>
                <% } %>

                <div class="material-links">
                  <% if (item.link_url) { %>
                    <a class="btn btn-outline-primary btn-sm" href="<%= item.link_url %>" target="_blank" rel="noopener noreferrer">Відкрити посилання</a>
                  <% } %>
                  <% if (item.file_path) { %>
                    <a class="btn btn-outline-light btn-sm" href="<%= item.file_path %>" target="_blank" rel="noopener noreferrer"><%= item.file_name || 'Відкрити файл' %></a>
                  <% } %>
                  <% if (isTeacherMode && item.can_manage) { %>
                    <form method="POST" action="/subjects/materials/<%= item.id %>/delete" class="d-inline">
                      <button class="btn btn-outline-danger btn-sm" type="submit" onclick="return confirm('Видалити цей матеріал?')">Видалити</button>
                    </form>
                  <% } %>
                </div>
              </article>
            <% }); %>
          <% } %>
        </section>
      <% } else { %>
        <div class="glass p-4 text-center text-muted">Оберіть предмет, щоб переглядати матеріали.</div>
      <% } %>
    </main>

    <%- include('partials/app-footer') %>
    <%- include('partials/mobile-menu-modal', { role, t, activePage: 'subjects' }) %>
    <%- include('partials/changelog-modal') %>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
    <script>
      const themeToggles = document.querySelectorAll('.theme-toggle');
      const savedTheme = localStorage.getItem('ui-theme') || 'theme-dark';
      document.body.classList.remove('theme-light', 'theme-dark');
      document.body.classList.add(savedTheme);
      themeToggles.forEach((btn) => {
        btn.textContent = savedTheme === 'theme-dark' ? btn.dataset.lightLabel : btn.dataset.darkLabel;
        btn.addEventListener('click', () => {
          const next = document.body.classList.contains('theme-dark') ? 'theme-light' : 'theme-dark';
          document.body.classList.remove('theme-light', 'theme-dark');
          document.body.classList.add(next);
          localStorage.setItem('ui-theme', next);
          themeToggles.forEach((b) => {
            b.textContent = next === 'theme-dark' ? b.dataset.lightLabel : b.dataset.darkLabel;
          });
        });
      });
    </script>
    <script src="/js/design-system.js" defer></script>
  </body>
</html>
