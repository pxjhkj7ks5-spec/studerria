<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Select Subjects</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <style>
      :root {
        --accent: #3b82f6;
        --accent-dark: #2563eb;
        --card-radius: 20px;
      }
      body {
        font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", "SF Pro Display",
          "Helvetica Neue", "Inter", Arial, sans-serif;
        min-height: 100vh;
      }
      body.theme-light {
        background: radial-gradient(1200px 800px at 20% 10%, rgba(147, 197, 253, 0.35), transparent 60%),
          radial-gradient(1000px 700px at 90% 20%, rgba(196, 181, 253, 0.35), transparent 60%),
          linear-gradient(135deg, #eef2ff 0%, #f0f9ff 50%, #fdf4ff 100%);
        color: #0f172a;
      }
      body.theme-dark {
        background: radial-gradient(1200px 800px at 15% 10%, rgba(139, 92, 246, 0.3), transparent 60%),
          radial-gradient(1000px 700px at 80% 20%, rgba(59, 130, 246, 0.25), transparent 60%),
          linear-gradient(160deg, #050508 0%, #12061a 45%, #1b0f26 100%);
        color: #f8fafc;
      }
      .auth-card {
        border-radius: var(--card-radius);
        border: 1px solid rgba(15, 23, 42, 0.08);
        box-shadow: 0 20px 60px rgba(15, 23, 42, 0.12);
      }
      body.theme-dark .auth-card {
        background: rgba(17, 17, 30, 0.9);
        border-color: rgba(255, 255, 255, 0.12);
      }
      .auth-card .form-control,
      .auth-card .form-check-input,
      .auth-card .form-select {
        border-radius: 12px;
        border: 1px solid rgba(15, 23, 42, 0.12);
        transition: border-color 150ms ease, box-shadow 150ms ease;
      }
      body.theme-dark .auth-card .form-control,
      body.theme-dark .auth-card .form-check-input,
      body.theme-dark .auth-card .form-select {
        background: rgba(255, 255, 255, 0.08);
        color: #f8fafc;
        border-color: rgba(255, 255, 255, 0.18);
      }
      body.theme-dark .auth-card h1,
      body.theme-dark .auth-card .h4 {
        color: #f8fafc;
      }
      body.theme-dark .auth-card .form-check-label,
      body.theme-dark .auth-card .fw-semibold {
        color: rgba(248, 250, 252, 0.92);
      }
      body.theme-dark .auth-card .form-check-input:checked {
        background-color: #3b82f6;
        border-color: #3b82f6;
      }
      body.theme-dark .auth-card .form-check-input {
        width: 1.1rem;
        height: 1.1rem;
        border-radius: 50%;
        background-color: transparent;
        box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.4);
      }
      body.theme-dark .auth-card .form-check-input:checked {
        box-shadow: inset 0 0 0 4px #0f172a, 0 0 0 1px #3b82f6;
      }
      body.theme-light .auth-card .form-check-input {
        width: 1.1rem;
        height: 1.1rem;
        border-radius: 50%;
        background-color: transparent;
        box-shadow: inset 0 0 0 1px rgba(15, 23, 42, 0.35);
      }
      body.theme-light .auth-card .form-check-input:checked {
        box-shadow: inset 0 0 0 4px #ffffff, 0 0 0 1px #3b82f6;
      }
      body.theme-light .subject-card {
        background: rgba(255, 255, 255, 0.8);
        border-color: rgba(15, 23, 42, 0.12) !important;
        box-shadow: 0 10px 24px rgba(15, 23, 42, 0.08);
      }
      body.theme-light .subject-card .fw-semibold {
        color: #0f172a;
      }
      .group-buttons {
        display: grid;
        gap: 12px;
        grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
      }
      .group-option {
        position: relative;
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 52px;
        border-radius: 14px;
        border: 1px solid rgba(15, 23, 42, 0.15);
        background: rgba(255, 255, 255, 0.7);
        font-weight: 600;
        cursor: pointer;
        transition: transform 140ms ease, box-shadow 140ms ease, border-color 140ms ease, background 140ms ease;
      }
      .group-option:hover {
        transform: translateY(-1px);
        box-shadow: 0 10px 22px rgba(15, 23, 42, 0.12);
        border-color: rgba(59, 130, 246, 0.6);
      }
      .group-option input {
        position: absolute;
        opacity: 0;
        pointer-events: none;
      }
      .group-option.is-selected {
        border-color: rgba(59, 130, 246, 0.9);
        background: rgba(59, 130, 246, 0.12);
        box-shadow: 0 10px 22px rgba(59, 130, 246, 0.2);
      }
      body.theme-dark .group-option {
        background: rgba(255, 255, 255, 0.08);
        border-color: rgba(255, 255, 255, 0.18);
        color: #f8fafc;
      }
      body.theme-dark .group-option.is-selected {
        background: rgba(59, 130, 246, 0.2);
        border-color: rgba(59, 130, 246, 0.9);
      }
      body.theme-dark .auth-card .border {
        border-color: rgba(255, 255, 255, 0.2) !important;
      }
      body.theme-dark .text-muted {
        color: rgba(255, 255, 255, 0.7) !important;
      }
      .auth-card .form-control:focus,
      .auth-card .form-check-input:focus,
      .auth-card .form-select:focus {
        border-color: var(--accent);
        box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.2);
      }
      .btn-primary {
        background: linear-gradient(180deg, var(--accent) 0%, var(--accent-dark) 100%);
        border: none;
        border-radius: 12px;
        padding: 0.75rem 1rem;
        font-weight: 600;
      }
      .theme-toggle-btn {
        border-radius: 999px;
      }
      .btn {
        transition: transform 140ms ease, box-shadow 140ms ease, border-color 140ms ease, background 140ms ease;
      }
      .btn:hover {
        transform: translateY(-1px);
        box-shadow: 0 10px 22px rgba(15, 23, 42, 0.12);
      }
      body.theme-dark .btn:hover {
        box-shadow: 0 10px 22px rgba(0, 0, 0, 0.4);
      }
      .btn:active {
        transform: translateY(0);
        box-shadow: none;
      }
      .app-footer {
        font-size: 0.8rem;
      }
      body.theme-dark .app-footer {
        color: rgba(255, 255, 255, 0.6);
      }
      body.theme-light .app-footer {
        color: rgba(15, 23, 42, 0.6);
      }
    </style>
  </head>
  <body class="theme-light">
    <div class="container py-4">
      <div class="row justify-content-center">
        <div class="col-12 col-lg-8">
          <div class="card auth-card">
            <div class="card-body p-4">
              <h1 class="h4 mb-3">Select Subject Groups</h1>
              <p class="text-muted">Choose a group for each subject (optional if not applicable).</p>

              <form method="POST" action="/register/subjects">
                <% if (!subjects.length) { %>
                  <div class="alert alert-info">No subjects available yet.</div>
                <% } else { %>
                  <% subjects.forEach(function(s) { %>
                    <% if (s.group_count > 1) { %>
                    <div class="border rounded p-3 mb-3 subject-card">
                      <div class="fw-semibold mb-2"><%= s.name %></div>
                      <div class="group-buttons">
                        <% for (let g = 1; g <= s.group_count; g++) { %>
                          <label class="group-option <%= s.default_group === g ? 'is-selected' : '' %>">
                            <input
                              type="radio"
                              name="subject_<%= s.id %>"
                              value="<%= g %>"
                              <%= s.default_group === g ? 'checked' : '' %>
                            />
                            Group <%= g %>
                          </label>
                        <% } %>
                      </div>
                    </div>
                    <% } %>
                  <% }); %>
                <% } %>

                <button type="submit" class="btn btn-primary">Finish Registration</button>
              </form>
            </div>
          </div>
          <div class="d-flex justify-content-center mt-3">
            <button class="btn btn-outline-secondary btn-sm theme-toggle-btn" type="button" id="themeToggle">Dark</button>
          </div>
          <footer class="app-footer text-center mt-3">
            <small>© <%= new Date().getFullYear() %> <%= authorName %> · v<%= appVersion %> · <%= buildStamp %></small>
          </footer>
        </div>
      </div>
    </div>
    <script>
      const themeToggle = document.getElementById('themeToggle');
      const savedTheme = localStorage.getItem('ui-theme') || 'theme-light';
      document.body.classList.remove('theme-light', 'theme-dark');
      document.body.classList.add(savedTheme);
      themeToggle.textContent = savedTheme === 'theme-dark' ? 'Light' : 'Dark';
      themeToggle.addEventListener('click', () => {
        const next = document.body.classList.contains('theme-dark') ? 'theme-light' : 'theme-dark';
        document.body.classList.remove('theme-light', 'theme-dark');
        document.body.classList.add(next);
        localStorage.setItem('ui-theme', next);
        themeToggle.textContent = next === 'theme-dark' ? 'Light' : 'Dark';
      });

      document.querySelectorAll('.group-buttons').forEach((group) => {
        group.addEventListener('click', (event) => {
          const label = event.target.closest('.group-option');
          if (!label) return;
          group.querySelectorAll('.group-option').forEach((el) => el.classList.remove('is-selected'));
          label.classList.add('is-selected');
        });
      });
    </script>
  </body>
</html>
